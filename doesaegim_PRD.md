# 📄 되새김 (Doesaegim) — 학습노트 요약·퀴즈 웹 서비스 PRD & 7일 실행 계획

> 수업 필기나 아티클을 **붙여넣으면 → Claude가 3줄 요약·핵심 용어·퀴즈 5문항으로 되돌려주는**
> 웹 서비스. 프론트는 순수 HTML/CSS/JS, 백엔드는 Vercel Serverless Functions(Python).

| 항목 | 내용 |
|------|------|
| 문서 버전 | v1 (2026-08-26) |
| 과제 | 2026년 AI활용학습 A1-3 |
| 작성자 | 김재민 |
| 이전 과제 | `../AI활용학습_A1_1`(Prompt Manager), `../AI활용학습_A1_2`(Trip Planner), `../AI활용학습_A2_1`(Brand Kit) |
| 서비스명 | **되새김 (Doesaegim)** — "붙여넣으면, 되새길 것만 남습니다" |
| 저장소명(예정) | `doesaegim` |

---

## 0. 확정 사항 (착수 전 결정)

과제 요구사항은 아이디어·API·구조를 전부 열어두었습니다. 아래는 **확정된 선택과 근거**입니다.
이 선택에 따라 파일 구조·프롬프트·오류 처리가 전부 달라지므로 착수 전에 고정합니다.

| 결정 항목 | 확정 | 근거 |
|-----------|------|------|
| 서비스 아이디어 | **학습노트 요약·퀴즈 생성기** | 입력(긴 텍스트)과 출력(구조화된 JSON)이 명확해 "입력 → 결과 출력" 요건을 증명하기 쉽고, 입력이 길어서 **타임아웃·길이 제한 처리**가 자연스럽게 학습 포인트가 됨 |
| AI API | **Claude API — 교육용 게이트웨이** `https://copa.codyssey.kr/v1/messages` | 과정에서 제공하는 엔드포인트. 규격은 §1.3 |
| 호출 방법 | **`requests`로 직접 HTTP 호출** (`anthropic` SDK 미사용) | 과정이 제공한 규격이 `requests` 기준이다. 게이트웨이가 SDK의 추가 헤더·파라미터까지 받아준다는 보장이 없으므로 **검증된 형태 그대로** 보낸다. 덤으로 번들이 작아 콜드 스타트가 빠르다 |
| 모델 | **`claude-haiku-4`** (환경 변수 `CLAUDE_MODEL`로 교체) | **Day 1 실측으로 확정.** 3,950자에 10.5초로 `claude-sonnet-4`(19~24초)의 절반이고 검증도 통과했다. 품질을 올리려면 값만 `claude-sonnet-4`로 바꾼다 |
| 요청 필드 | **`model`·`max_tokens`·`system`·`messages` 넷만 보낸다** | 규격에 있는 것이 이 넷이다. `temperature`·`thinking`·`output_config`는 보내지 않는다 (§6.3) |
| JSON 강제 방식 | **프롬프트 기반 + 코드블록 벗기기 + 1회 재시도** | 규격에 구조화 출력(`output_config`)이 없다. **A1-2·A2-1에서 쓰던 방식 그대로**라 학습이 이어진다 (§6.3) |
| 페이지 구성 | **단일 `index.html` + 해시 라우팅 4개 섹션** | 헤더·네비를 4개 파일에 복붙하지 않아도 되고, 네비 활성화 처리가 **JS의 역할**을 보여주는 실습이 됨 (§3.3) |
| 백엔드 구조 | **`api/` 파일 기반 함수** (`class handler(BaseHTTPRequestHandler)`) | 과제가 `api/` 폴더를 명시. Vercel의 프레임워크 프리셋(FastAPI/Flask)을 쓰면 `api/` 파일이 함수로 잡히지 않으므로 **쓰지 않는다** (§4.5) |
| 응답 방식 | **비스트리밍 (한 번에 JSON 반환)** | 스트리밍은 `BaseHTTPRequestHandler`에서 청크 전송·프론트 파서까지 따라와서, 이 과제의 학습 목표(fetch → 응답 → 화면 반영)를 흐린다. §6.3 출력은 4,096 토큰 안에 들어옴 |
| 보너스 | **A. 다크 모드 + 마이크로 인터랙션 / B. 결과 저장 + 알림 자동화** 둘 다 채택 | §10, §11 |
| 결과 저장 위치 | **localStorage(본문 포함).** 외부 전송 없음 | 로그인이 없으므로 서버에 두지 않는다 (§11.1) |

> ⚠️ **해시 라우팅으로 확정했으므로** 섹션 추가는 `<section id="...">` 하나와 네비 링크 하나로 끝납니다.
> 나중에 멀티 페이지(`study.html` 등)로 바꾸려면 헤더·네비·테마 스크립트를 전부 복제해야 하므로,
> 바꾸려면 **Day 3 이전에** 결정해야 합니다.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 이름 | 되새김 (Doesaegim) |
| 한 줄 정의 | 학습 노트를 붙여넣으면 요약·핵심 용어·퀴즈로 되돌려주는 웹 서비스 |
| 프론트 | 순수 HTML / CSS / JavaScript (프레임워크 금지) |
| 백엔드 | Vercel Serverless Functions — Python 3.12 |
| AI | Claude API — 교육용 게이트웨이 (`requests`로 직접 호출) |
| 배포 | GitHub → Vercel 자동 배포 |
| 산출물 | 배포 URL, GitHub 저장소, README.md, 서비스 기획서, 증빙 자료(스크린샷 + AI 도구 로그) |

### 1.1 이전 과제와 달라지는 점

| 항목 | A1-2 / A2-1 (CLI) | **A1-3 (웹 서비스)** |
|------|------------------|---------------------|
| 실행 형태 | 내 PC 터미널 | **브라우저 → 인터넷 → Vercel 서버** |
| 키가 있는 곳 | 내 PC의 `.env` | **Vercel 서버의 환경 변수** (브라우저에는 절대 없음) |
| 사용자 | 나 혼자 | **동료 누구나** (URL만 알면 접속) |
| 실패의 의미 | 내가 다시 실행하면 됨 | **모르는 사람이 빈 화면을 본다** → 안내 문구가 기능의 일부 |
| 새 난이도 | — | **로컬은 되는데 배포가 안 되는 상황**을 스스로 진단하기 |

> A1-2에서 배운 "부분 실패해도 결과는 만든다"는 여기서 **"실패해도 사용자는 무엇을 해야 할지 안다"**로
> 바뀝니다. CLI는 로그를 읽을 사람이 나뿐이지만, 웹은 아무 설명 없이 멈추면 사용자가 새로고침밖에 할 게 없습니다.

### 1.2 실제 환경 확인 결과 (2026-08-26 기준, 공식 문서 확인)

착수 전에 사실로 확인한 것입니다. **추측이 아니라 문서에 적힌 값**이므로 여기에 맞춰 설계합니다.

| 항목 | 확인된 값 | 출처 / 영향 |
|------|-----------|-------------|
| `api/*.py` 함수 진입점 | 파일에 **`handler`** 라는 이름의 `BaseHTTPRequestHandler` 하위 클래스를 정의 (또는 `app`/`application`) | Vercel Python `/api` 문서. 클래스 이름을 `Handler`로 대문자로 쓰면 인식 안 됨 |
| 라우팅 | `api/summarize.py` → `/api/summarize` | 파일 경로가 곧 URL |
| 유틸 파일 제외 규칙 | `_`로 시작하거나 `.`으로 시작하는 파일은 **함수로 만들어지지 않음** | `api/_shared.py`를 공용 모듈로 쓸 수 있음 (§4.2, 단 Day 2 검증 필요) |
| Python 버전 | **3.12(기본)** / 3.13 / 3.14 | `.python-version` 파일로 고정 |
| 의존성 | `requirements.txt` | **`requests`만** 넣는다 (`anthropic` SDK 미사용) |
| 프리셋 우선순위 | **프레임워크 프리셋이 감지되면 `/api` 파일은 함수가 되지 않는다** | `requirements.txt`에 `fastapi`/`flask`/`django`를 **절대 넣지 않는다** |
| 함수 최대 실행 시간 (Hobby) | 기본 **300초**, 최대 300초 (Fluid compute 기본 활성) | 우리는 `vercel.json`에서 **100초**로 잡는다 (§8.3) |
| `maxDuration` 설정 위치 | Python은 코드가 아니라 **`vercel.json`의 `functions`** | Node처럼 `export const maxDuration`은 통하지 않음 |
### 1.3 교육용 API 엔드포인트 (확정 규격)

과정에서 제공하는 게이트웨이를 사용합니다. **Anthropic Messages API 규격**입니다.

```python
# 이것이 우리 백엔드가 보낼 요청의 최종 형태다 (api/summarize.py — 부록 A)
import requests

response = requests.post(
    "https://copa.codyssey.kr/v1/messages",
    headers={
        "x-api-key": VIRTUAL_KEY,              # ★ os.environ에서만 읽는다 ★
        "anthropic-version": "2023-06-01",
    },
    json={
        "model": "claude-sonnet-4",
        "max_tokens": 1024,                    # Anthropic 규격에서는 필수
        "system": "당신은 친절한 학습 도우미입니다.",   # ★ messages가 아니라 최상위 필드 ★
        "messages": [{"role": "user", "content": "안녕하세요"}],
    },
    timeout=40,
)
print(response.json()["content"][0]["text"])    # OpenAI의 choices[0].message.content 와 위치가 다르다
```

| 항목 | 값 | 영향 |
|------|-----|------|
| URL | `https://copa.codyssey.kr/v1/messages` | 전체 경로를 그대로 쓴다 |
| 인증 헤더 | `x-api-key: <virtual-key>` | OpenAI식 `Authorization: Bearer`가 **아니다** |
| 버전 헤더 | `anthropic-version: 2023-06-01` | 빠뜨리면 400. 값을 임의로 올리지 않는다 |
| 요청 필수 필드 | `model`, `max_tokens`, `messages` | **`max_tokens`는 선택이 아니라 필수**다 |
| 시스템 프롬프트 | **최상위 `system` 필드** | `messages`에 `{"role":"system"}`을 넣는 OpenAI식이 아니다 |
| 응답 형태 | `content` **블록 배열** + `usage` | `data["content"][0]["text"]`. §4.4·§6.4·부록 A가 이 형태를 가정한다 |
| 모델 ID | `claude-sonnet-4` / `claude-haiku-4` | 게이트웨이가 제공하는 ID를 그대로 쓴다. 날짜 접미사를 임의로 붙이지 않는다 |

> 🚨 **과정이 함께 준 JavaScript(`fetch`) 예시를 `js/`에 그대로 넣으면 안 됩니다.**
> 그 예시는 **API의 모양을 설명하기 위한 것**입니다. 브라우저에서 실행되는 코드에
> `x-api-key`를 쓰면 **개발자 도구 네트워크 탭에 virtual key가 그대로 보입니다.**
> 우리 프론트의 `fetch`는 `https://copa.codyssey.kr`이 아니라 **`/api/summarize`(우리 서버)**로
> 갑니다. 키를 아는 곳은 `api/*.py`뿐입니다. 자세한 이유는 §9.1.
>
> **이것이 이 과제에서 서버리스 함수를 쓰는 이유 그 자체입니다.**
> "브라우저 예시가 있으니 그대로 쓰면 되지 않나?"가 정확히 이 과제가 걸러내려는 실수입니다.

> ⚠️ 규격에 **구조화 출력(`output_config`)·`thinking`·`temperature`가 없습니다.**
> 그래서 §6.3의 JSON 강제는 **프롬프트 기반**으로 확정합니다. 규격에 없는 필드를
> "혹시 되지 않을까" 하고 넣지 마십시오 — 게이트웨이가 400을 주거나 조용히 무시합니다.

---

## 2. 요구사항 추적표 (과제 요건 ↔ 대응 계획)

### 2.1 제출 패키지 (필수 5종)

| # | 요건 | 대응 | 문서 절 |
|---|------|------|---------|
| 1 | 배포된 웹 서비스 (Vercel URL) | GitHub 연동 자동 배포 | §12 |
| 1-a | 최소 3개 이상 페이지/섹션 + 메뉴 이동 | **4개 섹션** (홈 / 되새김 / 내 기록 / 가이드·문의) + 해시 라우팅 | §3.3 |
| 1-b | 반응형 (모바일 정상 표시) | 3개 브레이크포인트 + 실제 기기·DevTools 확인 | §5.3, §14.2 |
| 1-c | AI API 연동 기능 1개 이상 (입력→출력) | `POST /api/summarize` | §6, §7.1 |
| 2 | GitHub 저장소 + 커밋 이력 | 기능 단위 커밋, `type: 한국어 설명` | §16 |
| 2-a | 프론트/백엔드 구조 구분 | `index.html`·`css/`·`js/` ↔ `api/` | §4.2 |
| 3 | README.md (소개·스택·실행/배포·URL·환경 변수) | Day 6 작성 | §15 |
| 4 | 서비스 기획서 (목적·타겟·페이지·핵심 기능 + AI 입출력/실패 기준) | `docs/서비스기획서.md` — 본 PRD §3·§6·§8을 발췌·재구성 | §3 |
| 5 | 증빙 (스크린샷 데스크톱+모바일+AI 동작 / AI 코딩 도구 로그) | `docs/screenshots/`, `docs/ai-tool-log/` | §14.3 |

### 2.2 기능 요구사항 (§4. 기능 요구 사항 1~8)

| # | 요건 | 대응 | 절 |
|---|------|------|-----|
| 1 | 서비스 기획 (아이디어·목적·타겟·섹션 3개↑·AI 기능 1개↑) | 확정 완료 | §3 |
| 2 | 프로젝트 초기화 및 구조 구성 | `index.html`, `css/`, `js/`, `api/`, `images/`, `requirements.txt` | §4.2 |
| 3 | 프론트엔드 화면 구현 (바닐라) + 네비게이션 + 레이아웃 | 해시 라우터 + CSS 변수 기반 스타일 | §5 |
| 4 | 반응형 (최소 2가지 화면 크기 확인) | 375 / 768 / 1280 **3종 확인** | §5.3, §14.2 |
| 5 | AI 기능 UX 최소 기준 (입력 UI / 결과 표시 / 실패 안내 1개↑) | **3개 전부** 구현: 빈 입력·API 오류·타임아웃 | §8.2 |
| 6 | AI API 연동 (Python 함수, requirements.txt, fetch) | `api/summarize.py` + `js/api.js` | §7 |
| 7 | 배포 및 동작 검증 | Day 2에 "빈 껍데기 먼저 배포" → 매일 재배포 | §12, §13 |
| 8 | 문서화 및 제출 패키지 | README·기획서·증빙 | §15 |

### 2.3 보너스 (선택 — 둘 다 채택)

| # | 요건 | 대응 | 절 |
|---|------|------|-----|
| 1 | 운영 자동화 / 데이터 저장 고도화 | **결과 저장 — localStorage 최근 10건**(열람·삭제·다시보기). 웹훅 문의 폼은 착수 후 철회 — 사유는 §11.2 | §11 |
| 2 | UX 및 측정 고도화 | 다크 모드 + 마이크로 인터랙션(스켈레톤·버튼 상태·토스트) | §10 |

### 2.4 개발 환경 · 제약 사항 (과제 §6·§7)

**이 표가 없으면 "만들긴 했는데 요건을 어겼다"가 됩니다.** 기능이 아니라 조건이라서
체크리스트에서 빠지기 쉬운 항목들입니다.

| 구분 | 요건 | 대응 | 절 |
|------|------|------|-----|
| 환경 | 프론트는 **순수 HTML/CSS/JS** (React/Vue 금지) | 프레임워크·빌드 도구 없음. `<script src>` 7개로 끝 | §0, 부록 C |
| 환경 | 백엔드는 **Vercel Serverless Functions (Python), `api/` 폴더** | `api/summarize.py`·`health.py` | §4.2, §7 |
| 환경 | AI 기능 **1개 이상** | `/api/summarize` (1개) | §6 |
| 보안 | API 키는 **환경 변수로 관리**, 코드·README·스크린샷에 노출 금지 | `os.environ`만 사용 + 9항목 점검 **2회** | §9.1~§9.4 |
| 보안 | **과금/쿼터 인지**, 호출 빈도와 실패 상황 고려 | 입력 4,000자 상한 + 3중 호출 제한 + `usage` 실측 | §9.6 |
| 보안 | 키 유출 시 **폐기·재발급 + 노출된 커밋 이력 정리** | 4단계 절차 + 이력 정리 3안 | §9.5 |
| 제출 | 제출 패키지 **5종 누락 없이** | §15 체크리스트 | §15 |
| 제출 | **템플릿/예시를 그대로 복제하지 않는다** (아이디어·문구·구성 변경 필수) | §2.5 자기 점검 | §2.5 |
| 제출 | 배포 URL에서 **동작이 재현 가능** (동료가 접속·테스트 가능) | Day 7에 동료 1명 실제 접속 확인 | §13 Day 7 |

### 2.5 "템플릿 복제가 아님"을 스스로 증명하기

과제가 명시적으로 금지한 항목이라 **말이 아니라 근거로** 답할 수 있어야 합니다.
아래 5가지는 어떤 예제에서도 그대로 나오지 않는, 이 서비스만의 결정입니다.

| 항목 | 이 프로젝트의 선택 | 흔한 예제와 다른 점 |
|------|-------------------|---------------------|
| 아이디어 | 학습 노트 → **요약 + 용어 + 퀴즈 5문항** | 대개는 "챗봇" 또는 "요약기" 하나에서 끝난다. **퀴즈 생성**이 핵심 가치(§3.5) |
| 이름·문구 | 되새김 / "붙여넣으면, 되새길 것만 남습니다" | 되새김질(반추) = 인출 연습이라는 학습법 은유. 기능명이 아니다 |
| 구성 | 홈 / 되새김 / **내 기록** / 가이드·문의 | "내 기록"은 로그인 없이 localStorage로 푼 결과 — 이 제약에서 나온 구성(§11.1) |
| 데이터 모델 | O/X도 `options: ["O","X"]`로 통일 | 스키마 단순화를 위해 **일부러 정규화하지 않은** 설계 결정(§6.3) |
| 실패 처리 | E1~E18 + 코드→문구 분리(§8.4) | 예제는 대개 `alert("오류")` 하나로 끝난다 |

> README의 **"설계 결정 (왜 이렇게 만들었는가)"** 절이 이 표를 근거로 작성됩니다.
> 대안을 나열하고 각각의 문제점을 적은 뒤 채택안을 표시하는 형식입니다(A1-1부터의 관례).

---

## 3. 서비스 기획 (제출물 4번의 원본)

> `docs/서비스기획서.md`는 이 절을 사람이 읽기 좋게 다듬은 문서입니다.
> **여기가 원본**이고, 내용이 갈리면 이 문서를 기준으로 합니다.

### 3.1 목적 — 어떤 문제를 푸는가

노트 필기는 **쓰는 순간이 아니라 다시 볼 때** 효과가 납니다. 그런데 실제로는

- 필기해 놓고 다시 안 본다 (분량이 많아 어디부터 볼지 모른다)
- 다시 읽어도 "읽었다"는 느낌만 남고 **기억에 남지 않는다** (인출 연습 없이 재독만 함)

되새김은 이 두 문제를 **하나의 입력**으로 해결합니다. 노트를 붙여넣으면
① 어디부터 볼지 알려주는 **3줄 요약**, ② 뭘 외워야 하는지 알려주는 **핵심 용어**,
③ 읽기가 아니라 **떠올리기**를 시키는 **퀴즈 5문항**을 한 번에 만들어 줍니다.

> 서비스명 "되새김"은 되새김질(반추)에서 왔습니다. 한 번 삼킨 내용을 다시 꺼내 씹어야
> 소화된다는 학습법(인출 연습, retrieval practice)을 그대로 이름으로 썼습니다.

### 3.2 타겟 사용자

| 구분 | 1순위 | 2순위 |
|------|-------|-------|
| 누구 | **시험·자격증을 준비하는 학생 / 부트캠프 수강생** | 아티클·문서를 많이 읽는 직장인 |
| 상황 | 수업 직후, 필기는 있는데 복습할 시간이 30분뿐 | 읽은 아티클을 나중에 다시 못 찾음 |
| 지금 하는 일 | 노트를 처음부터 다시 읽는다 | 북마크만 하고 끝 |
| 불편 | 분량 대비 시간이 없고, 읽어도 남지 않는다 | 요약이 없어 다시 열어보지 않는다 |
| 되새김이 주는 것 | **30초 만에** 요약 + 외울 것 + 확인 문제 | 요약과 용어가 남아 재방문 가치가 생김 |

**대표 사용 시나리오 (Day 7 검증·스크린샷 촬영 시나리오와 동일 — §14.3)**

1. 수업이 끝나고 노트 앱에서 오늘 필기를 전체 복사한다.
2. 되새김 → [되새김] 탭에 붙여넣고 과목을 "IT", 난이도를 "보통"으로 고른다.
3. 15초쯤 뒤 요약 3줄 · 용어 4개 · 퀴즈 5문항이 나온다.
4. 퀴즈를 풀면 즉시 채점되고 해설이 펼쳐진다.
5. [내 기록]에 자동 저장돼서 지하철에서 다시 열어본다.

### 3.3 페이지 구성 & 네비게이션 (요건: 3개 이상)

**단일 `index.html` + 해시 라우팅 4개 섹션.** 상단 고정 네비의 링크를 누르면 URL이
`#/study`처럼 바뀌고, JS가 해당 섹션만 보여줍니다.

| # | 섹션 | 해시 | 하는 일 | AI |
|---|------|------|---------|:--:|
| 1 | **홈** | `#/` (기본) | 서비스 소개, 3단계 사용법, "지금 되새기기" CTA, 샘플 결과 미리보기 | — |
| 2 | **되새김** | `#/study` | **AI 기능.** 노트 입력 → 요약·용어·퀴즈 출력, 퀴즈 채점 | ✅ |
| 3 | **내 기록** | `#/history` | localStorage에 저장된 최근 10건 목록·다시보기·삭제·전체 비우기 | — |
| 4 | **가이드 · 문의** | `#/guide` | 되새김을 잘 쓰는 법(분산 반복·인출 연습) + FAQ + 문의 안내(GitHub 이슈) | — |

**네비게이션 규칙**

- 데스크톱: 상단 가로 메뉴. 현재 섹션은 `aria-current="page"` + 밑줄 강조.
- 모바일(<600px): 햄버거 버튼 → 전체 화면 오버레이 메뉴. 링크를 누르면 자동으로 닫힌다.
- 브라우저 **뒤로 가기**로도 섹션이 바뀌어야 한다 → `hashchange` 이벤트로 처리 (§4.3).
- 알 수 없는 해시(`#/asdf`)는 홈으로 되돌린다.

### 3.4 핵심 기능

| 기능 | 설명 | 위치 |
|------|------|------|
| **노트 되새김 (AI)** | 노트 텍스트 + 과목 + 난이도 → 요약 3줄 / 용어 3~5개 / 퀴즈 5문항 | `#/study` |
| 퀴즈 채점 | 5문항을 풀고 [채점하기] → 정오 표시 + 해설 펼침 + 점수 | `#/study` |
| 결과 복사 | 결과 전체를 Markdown으로 클립보드 복사 | `#/study` |
| 내 기록 | 결과를 브라우저에 자동 저장, 최근 10건 열람·삭제 | `#/history` |
| 문의 안내 | GitHub 이슈 링크. **개인정보를 받지 않는다** (§11.2) | `#/guide` |
| 다크 모드 | 시스템 설정 자동 감지 + 수동 토글 기억 (보너스) | 전역 |

### 3.5 AI 기능이 사용자에게 주는 가치

| 사용자가 직접 하면 | 되새김이 대신하면 |
|--------------------|-------------------|
| 노트 재독 15~20분 | 요약 3줄 읽기 30초 |
| 뭘 외워야 할지 스스로 판단 | 핵심 용어 3~5개가 뽑혀 나옴 |
| 문제를 만들 수 없음 (스스로 낸 문제는 이미 답을 앎) | **제3자 시점의 퀴즈 5문항** — 인출 연습이 가능해짐 |

> 퀴즈 생성이 이 서비스의 진짜 가치입니다. 요약만이면 "긴 글 줄여주기"와 다르지 않습니다.

---

## 4. 시스템 구조

### 4.1 요청 흐름 — 과제 목표 "설명할 수 있어야 한다"의 핵심 그림

```
[브라우저]                                   [Vercel]                        [Anthropic]
  index.html   ─ HTML: 무엇이 있는가(구조)
  css/*.css    ─ CSS : 어떻게 보이는가(표현)
  js/study.js  ─ JS  : 무엇이 일어나는가(동작)
       │
       │ ① 사용자가 <textarea>에 노트를 붙여넣고 [되새기기] 클릭
       │    submit 이벤트 → preventDefault() → 값 검증
       ▼
  js/api.js
       │ ② fetch('/api/summarize', {
       │        method: 'POST',
       │        headers: {'Content-Type': 'application/json'},
       │        body: JSON.stringify({ note, category, level }),
       │        signal: AbortSignal.timeout(90000)     ← 지연 대비
       │    })
       │
       │  ※ 같은 도메인이므로 절대 URL도 CORS 설정도 필요 없다
       ▼
  ─────────────── 인터넷 ───────────────
                                    api/summarize.py
                                     │ ③ do_POST() → body 읽기 → json.loads
                                     │ ④ 입력 검증 (길이/타입)  → 400
                                     │ ⑤ os.environ["ANTHROPIC_API_KEY"]
                                     │    ★ 키는 여기서만 존재한다 ★
                                     │ ⑥ requests.post(COPA_URL, ...)  ─────►  Claude
                                     │                                          │
                                     │ ⑦ 응답 JSON 검증 (스키마 통과 ≠ 검증 통과) ◄──┘
                                     │ ⑧ self.send_response(200)
                                     │    self.wfile.write(json.dumps(...))
       ┌─────────────────────────────┘
       ▼
  js/study.js
       │ ⑨ const data = await res.json()
       │ ⑩ document.createElement / textContent 로 DOM 생성 → 화면 반영
       ▼
  [화면에 요약·용어·퀴즈가 나타남]
```

**이 그림에서 반드시 말로 설명할 수 있어야 하는 것 4가지**

1. HTML은 구조, CSS는 표현, JS는 동작 — ①에서 셋이 각각 무슨 일을 하는가
2. ②의 `fetch`는 사용자의 클릭을 **HTTP 요청**으로 바꾸는 지점이다
3. ⑤ 키가 브라우저가 아니라 **서버에만** 있는 이유 (§9.1)
4. ⑩ 응답이 **문자열(JSON)**로 오고, JS가 그걸 **DOM 요소**로 바꿔야 화면에 보인다

### 4.2 파일 구조

```
AI활용학습_A1_3/                 ← GitHub 저장소 루트 (repo: doesaegim)
├─ index.html                    ← 유일한 HTML. 4개 <section>을 모두 포함
├─ css/
│  ├─ base.css                   ← 리셋 · CSS 변수(라이트/다크) · 타이포그래피
│  ├─ layout.css                 ← 헤더 · 네비 · 섹션 · 그리드 · 미디어 쿼리
│  └─ components.css             ← 버튼 · 카드 · 폼 · 퀴즈 · 스켈레톤 · 토스트
├─ js/
│  ├─ router.js                  ← 해시 라우팅 + 네비 활성화 + 모바일 메뉴
│  ├─ theme.js                   ← 다크 모드 토글 + localStorage 기억
│  ├─ api.js                     ← fetch 래퍼 (타임아웃 · 에러코드 → 한국어 문구)
│  ├─ study.js                   ← AI 폼 제출 · 결과 렌더 · 퀴즈 채점
│  ├─ history.js                 ← localStorage 기록 저장/목록/삭제
│  └─ mock.js                    ← 서버 없이 화면만 볼 때 쓰는 고정 응답 (§12.3)
├─ api/
│  ├─ summarize.py               ← POST /api/summarize   ★ AI 기능
│  ├─ health.py                  ← GET  /api/health      (배포 진단용)
│  └─ _shared.py                 ← 공용 헬퍼 (밑줄로 시작 → 함수로 안 잡힘)
├─ images/                        ← 과제 요건 2의 명시 폴더. 실제로 쓰이게 한다
│  ├─ logo.svg                   ← 헤더 로고 + favicon
│  └─ og-cover.png               ← og:image (배포 URL 공유 시 미리보기)
├─ docs/
│  ├─ 서비스기획서.md             ← 제출물 4번
│  ├─ screenshots/               ← 제출물 5번 (01~08)
│  └─ ai-tool-log/               ← 제출물 5번 (AI 코딩 도구 대화 로그)
├─ requirements.txt
├─ vercel.json
├─ .python-version               ← "3.12"
├─ .env.example                  ← 키 "이름"만. 값은 비워둔다
├─ .gitignore
├─ README.md
├─ CLAUDE.md
└─ doesaegim_PRD.md              ← 이 문서
```

> **`api/_shared.py` 주의** — 밑줄 파일이 라우트가 되지 않는 것은 문서로 확인했지만,
> `from _shared import ...` **import가 되는지는 실제 배포로 확인해야 합니다.**
> Day 2에 가장 단순한 `api/health.py`에서 먼저 import를 시도해 배포합니다.
> 실패하면 ① `sys.path.append(os.path.dirname(__file__))` 추가 → ② 그래도 안 되면
> **세 파일을 각각 자체 완결로 되돌립니다**(중복 30줄이 배포 실패보다 낫습니다).
> 결과는 `CLAUDE.md`의 "측정으로 확정된 것"에 기록합니다.

### 4.3 프론트 모듈 계약

각 JS 파일이 **밖으로 노출하는 것**만 적습니다. 이 계약을 지키면 파일 하나를 고쳐도 나머지가 안 깨집니다.

```js
// js/router.js
initRouter()                       // DOMContentLoaded에서 1회 호출. hashchange 구독
navigateTo(hash)                   // 프로그램에서 섹션 이동 (홈 CTA → #/study)

// js/theme.js
initTheme()                        // 저장값 → 없으면 prefers-color-scheme
toggleTheme()                      // 버튼 클릭

// js/api.js
callApi(path, payload, {timeoutMs}) // → { ok, data } | { ok:false, code, message }
                                    // ★ 절대 throw 하지 않는다. 항상 위 모양으로 반환 ★
MESSAGES                            // 에러 코드 → 한국어 안내 문구 사전 (§8.4)

// js/study.js
initStudyForm()                    // submit 구독, 렌더, 채점
renderResult(data)                 // 결과 → DOM (재사용: 내 기록의 '다시 보기')

// js/history.js
saveResult(data)                   // 최근 10건 유지
listResults() / removeResult(id) / clearResults()

```

> **`callApi`가 예외를 던지지 않는 계약**은 A2-1의 `llm.generate_json`에서 이어옵니다.
> 호출하는 쪽(`study.js`)에 `try/catch`가 흩어지지 않고,
> 실패 처리를 **한 군데(§8.4 문구표)**에서 관리할 수 있습니다.

### 4.4 백엔드 함수 계약

세 함수 모두 아래 모양을 지킵니다.

```python
class handler(BaseHTTPRequestHandler):
    def do_POST(self):   # health.py만 do_GET
        ...
    def do_OPTIONS(self):        # 같은 도메인이라 필요 없지만, 405로 조용히 닫는다
        ...
```

**응답 규약 (성공/실패 모두 JSON, 절대 HTML 오류 페이지를 내지 않는다)**

```jsonc
// 성공
{ "ok": true,
  "data": { /* §6.3 스키마 */ },
  "meta": { "model": "claude-sonnet-4", "elapsed_ms": 8410,
            "input_tokens": 4210, "output_tokens": 1780 } }

// 실패
{ "ok": false, "code": "UPSTREAM_TIMEOUT", "message": "응답이 너무 오래 걸렸습니다. ..." }
```

> `meta.input_tokens` / `output_tokens`는 **과금이 얼마나 나가는지 눈으로 보기 위한** 값입니다.
> 화면에는 표시하지 않고 Vercel 로그와 개발자 도구에서만 확인합니다 (§9.6).

### 4.5 왜 프레임워크(FastAPI/Flask)를 쓰지 않는가

Vercel은 `requirements.txt`에서 FastAPI·Flask·Django를 발견하면 **프레임워크 프리셋**으로 전환하고,
그때부터 **`/api` 폴더의 `.py` 파일들은 개별 함수가 되지 않습니다.** 과제는 `api/`에 함수를 구현하라고
명시했으므로, `requirements.txt`에는 `requests`만 넣습니다.

> 자주 나오는 사고: AI 도구가 "더 편하다"며 `fastapi`를 `requirements.txt`에 추가 → 배포는 성공 →
> `/api/summarize`가 **404**. 원인이 코드가 아니라 의존성 목록에 있어서 찾기 어렵습니다.

---

## 5. 화면 설계

### 5.1 공통 레이아웃

```
┌──────────────────────────────────────────────────────────┐
│  🌀 되새김        홈  되새김  내 기록  가이드·문의   [🌙] │ ← 상단 고정(sticky)
├──────────────────────────────────────────────────────────┤
│                                                          │
│                   (현재 섹션 하나만 표시)                 │
│                     최대 폭 1080px 중앙                   │
│                                                          │
├──────────────────────────────────────────────────────────┤
│  되새김 · 2026 AI활용학습 A1-3 · 김재민 · GitHub          │ ← 푸터
└──────────────────────────────────────────────────────────┘
```

### 5.2 섹션별 와이어프레임

**① 홈 `#/`**

```
   [히어로]  붙여넣으면, 되새길 것만 남습니다
             수업 필기를 3줄 요약 · 핵심 용어 · 퀴즈 5문항으로.
             [지금 되새기기 →]   ← 클릭 시 #/study 로 이동

   [3단계]   1 붙여넣기   2 과목·난이도 선택   3 요약·퀴즈 확인
             (카드 3개 — 데스크톱 가로 3열 / 모바일 세로 1열)

   [샘플]    실제 결과 예시 (정적 HTML, API 호출 없음)
```

**② 되새김 `#/study` — AI 기능**

```
   [입력 카드]
     과목  ( 일반 ▾ )      난이도  ( 쉬움 · 보통 · 어려움 ) ← 라디오
     ┌───────────────────────────────────────────┐
     │ 노트를 붙여넣으세요 (100자 이상 4,000자 이하) │
     │                                           │  ← textarea, 10줄
     └───────────────────────────────────────────┘
     0 / 4000 자   ← 실시간 카운터. 초과 시 빨강
     [ 되새기기 ]  [ 예시 넣기 ]

   [로딩]  스켈레톤 카드 3개 + "요약하는 중이에요… (보통 5~10초)"

   [결과]
     주제: 〈HTTP 요청과 응답〉
     ── 요약 ──────────  1. …  2. …  3. …
     ── 핵심 용어 ────    용어 카드 3~5개 (용어 / 뜻)
     ── 퀴즈 ─────────    Q1 (O/X)      ○ O   ○ X
                          Q2 (객관식)   ○ ① ○ ② ○ ③ ○ ④
                          …
                          [ 채점하기 ]  → 5문항 중 4개 정답 + 해설 펼침
     [ Markdown 복사 ]  [ 다시 만들기 ]

   [오류]  ⚠️ 아이콘 + 한국어 안내 + [다시 시도] 버튼   ← §8.4 문구표
```

**③ 내 기록 `#/history`**

```
   최근 10건까지 이 브라우저에 저장됩니다. (서버에 저장되지 않습니다)
   ┌──────────────────────────────────────────┐
   │ HTTP 요청과 응답        IT · 보통 · 8/28 │ [보기] [삭제]
   ├──────────────────────────────────────────┤
   │ 마케팅 4P               경영 · 쉬움 · 8/27│ [보기] [삭제]
   └──────────────────────────────────────────┘
   [ 전체 비우기 ]
   (비었을 때) 아직 기록이 없어요. [되새김에서 시작하기 →]
```

**④ 가이드 · 문의 `#/guide`**

```
   [되새김 200% 쓰는 법]  아코디언 3개
     · 오늘 만든 퀴즈는 내일 다시 풀기 (분산 반복)
     · 요약을 먼저 읽지 말고 퀴즈부터 (인출 연습)
     · 틀린 문항의 용어만 노트에서 다시 찾기
   [FAQ]  아코디언 4개 (내 노트가 저장되나요 / 왜 느린가요 / 무료인가요 / 틀린 퀴즈가 나와요)
   [문의]  "따로 문의 창구를 운영하지 않고 GitHub 이슈로 받습니다"
           [ GitHub에 이슈 남기기 → ]
```

### 5.3 반응형 브레이크포인트

| 이름 | 폭 | 레이아웃 |
|------|-----|----------|
| 모바일 | ~ 599px | 1열. 햄버거 메뉴. 폰트 15px. 버튼 폭 100%. textarea 8줄 |
| 태블릿 | 600 ~ 1023px | 2열 카드 그리드. 가로 메뉴 노출 |
| 데스크톱 | 1024px ~ | 최대 폭 1080px 중앙. 3열 카드. 폰트 16px |

**직접 확인할 3종 (요건은 2종 이상)**: iPhone 15 375×812 / iPad 768×1024 / 데스크톱 1280×800.

**모바일에서 자주 깨지는 지점 — 체크리스트**

- [ ] 가로 스크롤이 생기지 않는가 (`overflow-x: hidden`이 아니라 **원인**을 고칠 것)
- [ ] 긴 영어 단어·URL이 카드를 밀어내지 않는가 (`overflow-wrap: anywhere`)
- [ ] textarea 확대 방지 — 입력 폰트 **16px 이상** (iOS Safari는 16px 미만이면 자동 확대)
- [ ] 버튼 터치 영역 44×44px 이상
- [ ] 햄버거 메뉴가 열린 채 화면을 키우면 정상 복귀하는가

### 5.4 접근성 최소선

- 색상 대비 4.5:1 이상 (다크 모드에서도)
- 모든 `<input>`에 `<label>` 연결
- 결과 영역에 `aria-live="polite"` — 결과가 나왔을 때 스크린리더가 읽음
- 로딩 중 버튼 `disabled` + `aria-busy="true"`
- 포커스 링을 지우지 않는다 (`outline: none` 금지, 필요하면 `:focus-visible`로 다시 그림)

---

## 6. AI 기능 명세

### 6.1 입력 명세

| 필드 | 타입 | 필수 | 규칙 | 위반 시 |
|------|------|:----:|------|---------|
| `note` | string | ✅ | 공백 제거 후 **100자 이상 4,000자 이하** | `EMPTY_INPUT` / `TOO_SHORT` / `TOO_LONG` |
| `category` | string | — | `일반`·`IT`·`경영`·`과학`·`어학` 중 하나. 그 외/누락 → `일반` | 조용히 기본값 |
| `level` | string | — | `쉬움`·`보통`·`어려움`. 그 외/누락 → `보통` | 조용히 기본값 |

> **왜 100자 하한을 두는가** — 한 문장을 넣으면 요약할 것도 퀴즈로 낼 것도 없어서
> 모델이 내용을 지어냅니다. 결과 품질 문제를 **입력 검증으로** 막는 편이 낫습니다.
>
> **왜 4,000자 상한인가** — ① 응답 시간이 길어져 타임아웃 위험이 커지고,
> ② 입력 토큰이 곧 쿼터입니다 (§9.6). 넘치면 자르지 않고 **사용자에게 알립니다.**
> 조용히 자르면 사용자는 뒷부분이 요약에서 빠진 이유를 알 수 없습니다.

**검증은 프론트와 백엔드 양쪽에서 한다.**

| | 프론트 (`study.js`) | 백엔드 (`summarize.py`) |
|---|---|---|
| 목적 | 즉시 안내 (네트워크 낭비 방지) | **신뢰 경계.** 프론트는 우회 가능 |
| 방법 | 버튼 비활성 + 글자 카운터 + 제출 시 재확인 | `strip()` 길이 검사 → 400 |

> 프론트 검증만 두면 `curl`로 20만 자를 보낼 수 있고, 그건 곧 청구서입니다.
> **프론트 검증은 UX, 백엔드 검증은 보안**입니다. 둘은 대체 관계가 아닙니다.

### 6.2 프롬프트 설계

**system** (고정 문자열 — 캐시 친화적으로 앞쪽에 둔다)

```
너는 한국 학습자를 돕는 학습 코치다. 사용자가 준 학습 노트만을 근거로
요약·핵심 용어·확인 퀴즈를 만든다.

규칙
1. 노트에 없는 사실을 추가하지 않는다. 노트에서 확인할 수 없으면 만들지 않는다.
2. 요약은 정확히 3개이며, 각 항목은 한 문장(공백 포함 80자 이내)이다.
3. 핵심 용어는 3~5개다. 노트에 실제로 등장한 표현만 쓴다.
4. 퀴즈는 정확히 5문항이고, O/X 2문항 + 객관식 3문항이다.
5. 객관식 오답은 노트의 다른 내용에서 가져와 그럴듯하게 만든다.
   "위 보기 모두" 같은 회피형 보기를 쓰지 않는다.
6. 해설은 왜 그 답인지 노트의 근거를 들어 한두 문장으로 쓴다.
7. 모든 출력은 한국어다.
```

**user** (매 요청마다 달라지는 부분 — 뒤쪽에 둔다)

```
과목: {category}
  → {CATEGORY_HINT[category]}
난이도: {level}
  → {LEVEL_HINT[level]}

--- 학습 노트 시작 ---
{note}
--- 학습 노트 끝 ---
```

**★ 라벨이 아니라 지시를 넘긴다 (2026-08-31 실측으로 확정)**

처음에는 `과목: IT`처럼 **이름만** 넣었습니다. 같은 노트로 과목만 바꿔 3번 호출한 결과
**주제·용어·문항이 사실상 동일**했습니다 — `IT`로 두 번 돌려도 나오는 편차 수준.
**무엇을 다르게 하라는 지시가 없으면 모델은 라벨을 무시합니다.**

| 상수 | 예 |
|------|-----|
| `CATEGORY_HINT["IT"]` | 기술 용어는 원어를 함께 적는다(예: 메서드(method)). 동작 순서와 원인·결과를 묻는 문항을 우선한다. |
| `CATEGORY_HINT["경영"]` | 정의보다 '어떤 상황에 쓰는가'를 묻는다. 노트의 개념을 구체적인 사례에 적용하는 문항을 우선한다. |
| `LEVEL_HINT["어려움"]` | 적용·비교 수준. 노트의 개념을 새로운 상황에 적용하거나 여러 개념을 연결해야 풀리는 문항을 넣는다. |

수정 후 재측정 — **확실히 갈립니다.**

| 과목 | 실제로 달라진 것 |
|------|------------------|
| IT | 용어 원어 병기: `메서드(method)`, `상태 코드(status code)` |
| 어학 | 질문이 용법 중심: "어느 헤더에 **어떤 값을** 써야 하나?" |
| 경영 | 문항에 사례: "사용자가 **상품 목록 페이지**를 로드할 때…" |

`LEVEL_HINT`도 `"용어 확인 위주"`라는 **한 단어**에서 한 문장으로 늘렸습니다.
같은 이유로 너무 약했습니다.

> **왜 예시 JSON을 프롬프트에 넣지 않는가** — A1-2에서 실측으로 확인했습니다.
> 예시 블록을 넣으면 모델이 예시를 먼저 출력하고 진짜 답을 이어 붙여 JSON이 두 개가 됩니다.
> **구조는 프롬프트가 아니라 §6.3 스키마가 강제합니다.**

### 6.3 출력 JSON 스키마

```python
RESULT_SCHEMA = {
    "type": "object",
    "properties": {
        "topic":   {"type": "string"},
        "summary": {"type": "array", "items": {"type": "string"},
                    "minItems": 3, "maxItems": 3},
        "keywords": {
            "type": "array", "minItems": 3, "maxItems": 5,
            "items": {
                "type": "object",
                "properties": {"term": {"type": "string"},
                               "meaning": {"type": "string"}},
                "required": ["term", "meaning"],
                "additionalProperties": False,
            },
        },
        "quizzes": {
            "type": "array", "minItems": 5, "maxItems": 5,
            "items": {
                "type": "object",
                "properties": {
                    "type":        {"type": "string", "enum": ["ox", "choice"]},
                    "question":    {"type": "string"},
                    "options":     {"type": "array", "items": {"type": "string"},
                                    "minItems": 2, "maxItems": 4},
                    "answer":      {"type": "string"},
                    "explanation": {"type": "string"},
                },
                "required": ["type", "question", "options", "answer", "explanation"],
                "additionalProperties": False,
            },
        },
    },
    "required": ["topic", "summary", "keywords", "quizzes"],
    "additionalProperties": False,
}
```

> **이 스키마는 API로 보내지 않습니다.** 게이트웨이 규격(§1.3)에 구조화 출력이 없기 때문입니다.
> 대신 **① 프롬프트 지시문을 이 딕셔너리에서 생성**하고 **② §6.4 검증의 기준**으로 씁니다.
> 스키마를 코드에 남겨두는 이유가 이것입니다 — 지시문과 검증이 **한 정의에서 나와야** 어긋나지 않습니다.

**JSON 강제 방식 — 프롬프트 기반 (확정)**

§6.2의 system 프롬프트 끝에 아래 문단을 붙입니다.

```
출력 형식
반드시 아래 키를 가진 JSON 객체 하나만 출력한다.
코드블록 표시, 설명, 인사말을 앞뒤에 붙이지 않는다.
  topic     : 문자열
  summary   : 문자열 3개 배열
  keywords  : {term, meaning} 객체 3~5개 배열
  quizzes   : {type, question, options, answer, explanation} 객체 5개 배열
              type은 "ox" 또는 "choice", answer는 반드시 options 안의 값과 정확히 같다
```

> **예시 JSON 블록은 넣지 않습니다.** A1-2에서 실측으로 확인한 사실입니다 —
> 예시를 넣으면 모델이 예시를 먼저 출력하고 진짜 답을 이어 붙여 **JSON 객체가 두 개**가 됩니다.
> (예시 포함 5건 중 4건 실패 / 예시 없이 산문 지시 10건 중 0건 실패)
> 여기서는 API가 구조를 강제해 주지 않으므로 **이 규칙이 더 중요합니다.**

**호출 (§1.3 규격 그대로)**

```python
resp = requests.post(
    API_URL,                                  # https://copa.codyssey.kr/v1/messages
    headers={
        "x-api-key": os.environ["ANTHROPIC_API_KEY"],
        "anthropic-version": "2023-06-01",
    },
    json={
        "model": MODEL,                       # 기본 "claude-sonnet-4"
        "max_tokens": 4096,                   # 규격상 필수. 4,096이면 §6.3 출력이 들어간다
        "system": SYSTEM_PROMPT,              # ★ 최상위 필드 ★
        "messages": [{"role": "user", "content": user_prompt}],
    },
    timeout=40,                               # (§8.3 타임아웃 사다리 — 실측 반영)
)
```

**설계 원칙 — 규격에 있는 필드만 보낸다**

`temperature`, `thinking`, `output_config`는 **규격에 없습니다.** 게이트웨이가
400을 주거나 조용히 무시합니다. "혹시 되지 않을까" 하고 넣지 마십시오.
그리고 **우리 작업에는 셋 다 필요하지 않습니다** — 요약·퀴즈는 정형 작업이고,
출력 구조는 프롬프트 지시문 + §6.4 검증이 담당합니다.

**설계 결정 — 왜 O/X도 `options`를 갖는가**

`ox`와 `choice`를 서로 다른 모양으로 두면 스키마에 `oneOf`가 필요하고,
프론트에서도 분기가 늘어납니다. **O/X는 `options: ["O","X"]`인 객관식**으로 통일하면
스키마가 단순해지고 렌더링·채점 코드가 하나로 끝납니다. `type`은 화면 배지(O/X · 객관식) 표시용으로만 씁니다.

**금지 사항 (게이트웨이 규격 위반 — 400 또는 무시)**

| 하면 안 되는 것 | 이유 |
|-----------------|------|
| `max_tokens` 생략 | **Anthropic 규격에서는 필수.** 빠뜨리면 400 |
| `messages`에 `{"role":"system", ...}` | 시스템 프롬프트는 **최상위 `system` 필드**다 (OpenAI식과 다름) |
| `Authorization: Bearer ...` | 인증은 **`x-api-key`** 헤더다 |
| `anthropic-version` 헤더 생략·임의 변경 | `2023-06-01` 고정. 빠뜨리면 400 |
| `temperature` / `thinking` / `output_config` | **규격에 없다.** 400이거나 조용히 무시된다 |
| `data["choices"][0]["message"]["content"]` | OpenAI 규격이다. 여기선 **`data["content"][0]["text"]`** |
| 브라우저 `js/`에서 `copa.codyssey.kr` 직접 호출 | **virtual key가 네트워크 탭에 노출된다** (§1.3 🚨, §9.1) |

### 6.4 응답 처리 — 4단계

API가 구조를 강제해 주지 않으므로 **네 단계를 순서대로** 통과해야 데이터가 됩니다.
한 단계라도 건너뛰면 화면에서 `undefined`가 뜹니다.

**1단계 — 응답 껍데기에서 텍스트 꺼내기**

```python
data = resp.json()

# stop_reason을 content보다 먼저 본다 — 잘렸으면 JSON이 깨져 있다
if data.get("stop_reason") == "max_tokens":
    return fail("INVALID_AI_OUTPUT")          # max_tokens에 걸려 중간에 끊김

# [0]으로 바로 집지 않는다. 텍스트가 아닌 블록이 앞에 올 수 있다
text = next((b["text"] for b in data.get("content", []) if b.get("type") == "text"), None)
if not text:
    return fail("INVALID_AI_OUTPUT")
```

**2단계 — 코드블록 껍데기 벗기기**

"코드블록을 붙이지 말라"고 지시해도 모델은 종종 붙입니다. **지시는 요청이지 보장이 아닙니다.**

```python
def strip_fence(s: str) -> str:
    s = s.strip()
    if s.startswith("```"):
        s = s.split("\n", 1)[-1]              # 첫 줄(```json)을 버린다
        s = s.rsplit("```", 1)[0]             # 마지막 ```를 버린다
    return s.strip()
```

**3단계 — 파싱 (실패 시 정확히 1회 재시도)**

```python
try:
    result = json.loads(strip_fence(text))
except json.JSONDecodeError:
    # 재시도는 1회뿐이다. 과제 제약에 무한 재시도 금지가 명시돼 있다.
    # 재요청 시 프롬프트에 "JSON 객체 하나만" 문장을 한 줄 더 강조해 붙인다.
    ...
```

**4단계 — 의미 검증: "파싱 성공 ≠ 검증 성공"**

```python
def validate(data: dict) -> str | None:
    """문제가 있으면 사유 문자열, 없으면 None."""
    for key in ("topic", "summary", "keywords", "quizzes"):
        if key not in data:
            return f"필수 키 없음: {key}"
    if len(data["summary"]) != 3:
        return "요약이 3개가 아님"
    if not (3 <= len(data["keywords"]) <= 5):
        return "용어 개수 범위 밖"
    if len(data["quizzes"]) != 5:
        return "퀴즈가 5개가 아님"
    if not any(q.get("type") == "ox" for q in data["quizzes"]):
        return "ox 문항 없음"
    for q in data["quizzes"]:
        if q.get("answer") not in q.get("options", []):   # ← 가장 자주 깨지는 곳
            return "정답이 보기 안에 없음"
        if len(set(q["options"])) != len(q["options"]):
            return "보기 중복"
    return None
```

- **검증 실패 시: 재시도하지 않고 `INVALID_AI_OUTPUT`으로 안내한다.**
  3단계의 재시도는 **파싱 실패 전용**입니다. 의미 검증까지 재시도하면 응답 시간과
  쿼터 소모가 2배가 됩니다. "다시 시도" 버튼을 사용자에게 맡기는 편이 낫습니다.
- `stop_reason`이 `"refusal"`이면 `AI_REFUSED`로 안내합니다. 게이트웨이·모델에 따라
  이 값이 오지 않을 수도 있지만, **검사 두 줄을 남겨두면 나중에 모델을 올려도 안 깨집니다.**

> **`data["content"][0]["text"]`로 바로 집지 않는 이유** — 규격 예시는 그렇게 쓰지만,
> 블록 배열의 첫 원소가 항상 텍스트라는 보장은 없습니다. `next(...)`로 **타입을 확인하고**
> 꺼내면 코드가 한 줄 길어지는 대신 조용한 `KeyError`가 사라집니다.

### 6.5 결과 렌더링 규칙 (프론트)

- **`innerHTML`을 쓰지 않는다.** 모델 출력에 `<script>`가 섞여 들어오면 그대로 실행됩니다.
  `document.createElement` + `textContent`로만 만듭니다. (§9.4 점검 항목)
- 퀴즈 보기는 `<input type="radio" name="q{i}">` + `<label>`. 라벨 클릭으로도 선택돼야 합니다.
- 채점은 **전부 프론트에서** 합니다 (정답이 이미 응답에 들어 있음). 서버 왕복 없음.
- 채점 전에는 해설을 DOM에 넣지 않습니다 (개발자 도구로 미리 보는 걸 막기 위함 — 완벽하진 않지만 기본 예의).
- 채점 후 정답/오답 색상은 **색만으로 구분하지 않고** ✅/❌ 아이콘을 함께 씁니다 (색각 이상 대응).

---

## 7. API 명세 (백엔드)

### 7.1 `POST /api/summarize` ★ AI 기능

| | |
|---|---|
| 요청 | `{"note": "...", "category": "IT", "level": "보통"}` |
| 성공 | `200` + `{"ok": true, "data": {...}, "meta": {...}}` |
| 실패 | `4xx/5xx` + `{"ok": false, "code": "...", "message": "..."}` |
| 타임아웃 | `requests.post(..., timeout=40)` — 자동 재시도 없음 (§8.3) |

### 7.2 `GET /api/health` (배포 진단용)

```jsonc
{ "ok": true, "python": "3.12.x", "requests": "2.x.x",
  "has_api_key": true,          // ★ 키 값이 아니라 "설정됐는지"만 ★
  "model": "claude-sonnet-4",
  "custom_base_url": true,      // 교육용 엔드포인트를 쓰는지 여부만. ★ URL 자체는 넣지 않는다 ★
  "webhook_configured": false }
```

> 배포 후 `/api/summarize`가 실패할 때 원인이 **키 미설정인지 / 함수 배포 실패인지**를
> 1초 만에 가릅니다. `has_api_key`에 키의 앞 4글자 같은 걸 넣지 마십시오 —
> 그 순간 이 엔드포인트가 유출 경로가 됩니다.

### 7.3 오류 코드 ↔ HTTP 상태

| code | HTTP | 발생 지점 |
|------|:----:|-----------|
| `METHOD_NOT_ALLOWED` | 405 | GET으로 `/api/summarize` 호출 |
| `BAD_JSON_BODY` | 400 | body가 JSON이 아님 |
| `EMPTY_INPUT` | 400 | `note` 없음/공백 |
| `TOO_SHORT` / `TOO_LONG` | 400 | 100자 미만 / 4,000자 초과 |
| `RATE_LIMITED` | 429 | 우리 쪽 호출 제한 (§9.6) |
| `NO_API_KEY` | 500 | 서버에 `ANTHROPIC_API_KEY` 미설정 |
| `UPSTREAM_AUTH` | 500 | Claude 401/403 (키가 틀림·권한 없음) |
| `UPSTREAM_RATE_LIMITED` | 429 | Claude 429 |
| `UPSTREAM_TIMEOUT` | 504 | `APITimeoutError` / `APIConnectionError` |
| `UPSTREAM_ERROR` | 502 | Claude 5xx 및 그 밖의 API 오류 |
| `INVALID_AI_OUTPUT` | 502 | §6.4 검증 실패 |
| `AI_REFUSED` | 422 | `stop_reason == "refusal"` |
| `BAD_CONFIG` | 500 | `CLAUDE_MODEL`·`COPA_API_URL`이 **빈 값**으로 등록됨 (§12.4) |
| `INTERNAL` | 500 | 위 어디에도 안 걸린 예외 |

### 7.4 Vercel Python 함수 골격

```python
# api/summarize.py
from http.server import BaseHTTPRequestHandler

class handler(BaseHTTPRequestHandler):        # ★ 소문자 handler ★
    def do_POST(self):
        length = int(self.headers.get("Content-Length") or 0)
        raw = self.rfile.read(length)         # ★ 반드시 Content-Length만큼만 읽는다 ★
        ...
        self.send_response(200)
        self.send_header("Content-Type", "application/json; charset=utf-8")
        self.end_headers()
        self.wfile.write(json.dumps(body, ensure_ascii=False).encode("utf-8"))

    def do_GET(self):
        self._error(405, "METHOD_NOT_ALLOWED")
```

**여기서 실수하기 쉬운 3가지**

1. `self.rfile.read()`를 인자 없이 부르면 **요청이 끝날 때까지 블록**합니다. 반드시 `Content-Length`만큼.
2. `Content-Type`에 `charset=utf-8`이 없으면 한글이 깨져 보일 수 있습니다.
3. `ensure_ascii=False`를 빼면 한글이 `\uXXXX`로 나가서 응답이 3배로 커집니다(동작은 함).

전체 뼈대는 **부록 A**에 있습니다.

---

## 8. 예외 처리 명세

### 8.1 분류 원칙

| 구분 | 정의 | 처리 |
|------|------|------|
| **차단 (Blocked)** | 요청을 보낼 필요조차 없는 실패 | 프론트에서 즉시 안내, 네트워크 호출 안 함 |
| **거절 (Rejected)** | 서버가 받았지만 규칙 위반 | 4xx + 코드 + 문구 |
| **외부 실패 (Upstream)** | Claude API 쪽 문제 | 5xx + 코드 + **다시 시도 버튼** |
| **결과 불량 (Invalid)** | 응답은 왔는데 못 쓰는 내용 | 502 + 다시 시도 유도 |

> 원칙: **화면이 아무 말 없이 멈추는 경우는 0이어야 한다.**
> 로딩이 끝났는데 결과도 오류도 없는 상태가 가장 나쁜 실패입니다.

### 8.2 오류 표

| ID | 상황 | 구분 | 코드 | 처리 |
|----|------|:----:|------|------|
| E1 | 노트가 비었음 | 차단 | `EMPTY_INPUT` | 버튼 비활성 + textarea 아래 안내. **요건 5-①** |
| E2 | 100자 미만 | 차단 | `TOO_SHORT` | 글자 카운터 빨강 + "조금 더 붙여넣어 주세요" |
| E3 | 4,000자 초과 | 차단 | `TOO_LONG` | 초과분을 자르지 않고 안내 + 현재 글자 수 표시 |
| E4 | 10초 내 재요청 | 차단 | — | 버튼 쿨다운 (남은 초 표시) |
| E5 | body가 JSON이 아님 | 거절 | `BAD_JSON_BODY` | 400 |
| E6 | GET으로 호출 | 거절 | `METHOD_NOT_ALLOWED` | 405 |
| E7 | 우리 쪽 호출 제한 초과 | 거절 | `RATE_LIMITED` | 429 + "잠시 후 다시" |
| E8 | 서버에 키 미설정 | 외부 | `NO_API_KEY` | 500. 사용자에겐 "일시적 문제", **로그에는 명확히** |
| E9 | Claude 401/403 | 외부 | `UPSTREAM_AUTH` | 500. 로그에 "키 확인 필요" |
| E10 | Claude 429 | 외부 | `UPSTREAM_RATE_LIMITED` | 429 + "잠시 뒤 다시 시도" |
| E11 | Claude 5xx | 외부 | `UPSTREAM_ERROR` | 502 + 다시 시도 버튼. **요건 5-②** |
| E12 | `requests` 타임아웃 / 연결 실패 | 외부 | `UPSTREAM_TIMEOUT` | 504 + "노트를 조금 줄여보세요". **요건 5-③** |
| E13 | 프론트 `AbortController` 60초 만료 | 외부 | `CLIENT_TIMEOUT` | 로딩 해제 + 다시 시도 버튼 |
| E14 | 응답 JSON 검증 실패 (§6.4) | 불량 | `INVALID_AI_OUTPUT` | 502. **재시도하지 않음** |
| E15 | `stop_reason == "refusal"` | 불량 | `AI_REFUSED` | 422 + "다른 내용으로 시도해 주세요" |
| E16 | 그 밖의 예외 | 외부 | `INTERNAL` | 500. 예외 원문을 응답에 넣지 않는다 |
| E18 | localStorage 용량 초과 | 차단 | — | 가장 오래된 기록부터 삭제 후 재시도, 실패 시 토스트 |

> **E8·E9를 사용자에게 "API 키가 없습니다"라고 보여주지 않는 이유** — 서비스 내부 상태를
> 불특정 다수에게 알릴 이유가 없습니다. 사용자에겐 "일시적인 문제", **개발자에겐 Vercel 로그**로
> 나눕니다. `/api/health`가 이 진단을 대신합니다 (§7.3).

> **E12에 "노트를 줄여보세요"를 붙이는 이유** — 사용자가 **할 수 있는 행동**을 주는 안내가
> 좋은 오류 메시지입니다. "타임아웃이 발생했습니다"는 사용자가 할 게 없습니다.

### 8.3 타임아웃 사다리 (안쪽이 항상 더 짧다)

```
 requests.post(timeout=40)                      1회 호출 = 최대 40초
   └ JSON 파싱 실패 시 1회 재호출 (§6.4 3단계)   → 최악 80초
        └──────────────────────────────┐
 프론트 AbortController      90초       │ ← 서버가 먼저 실패해서 "친절한 JSON"을 주도록
        └──────────────────────────────┤
 Vercel maxDuration         100초       │ ← 마지막 안전망. 여기 걸리면 응답이 JSON이 아님
        └──────────────────────────────┘
```

> **이 숫자는 Day 1 실측에서 나왔습니다 (2026-08-26).** 3,950자 노트 기준
> `claude-haiku-4` **10.5초** / `claude-sonnet-4` **19~24초(편차 큼)**.
> 처음 잡았던 25초는 sonnet이 간헐적으로 넘겨서 **40초로 올렸고**, 바깥 두 층도 함께 밀었습니다.
> Hobby 플랜의 `maxDuration` 상한이 300초이므로 100초는 여유 안에 있습니다.

**순서가 뒤집히면 생기는 일**: 프론트가 20초에 끊으면, 서버는 25초에 성공한 응답을
**아무도 안 받는 곳으로** 보냅니다. 사용자는 실패로 보는데 **쿼터는 소모됩니다.**
그래서 **바깥쪽이 항상 더 길어야** 합니다.

> **`requests`는 자동 재시도를 하지 않습니다.** SDK를 쓸 때와 달리 재시도 횟수를
> 우리가 직접 셉니다 — 그래서 최악의 대기 시간이 `25 × 2 = 50초`로 명확하게 계산됩니다.
> **재호출은 §6.4 3단계(파싱 실패)에서만 1회입니다.** 타임아웃·5xx는 재시도하지 않고
> 바로 사용자에게 안내합니다 (같은 이유로 또 실패할 가능성이 높고, 대기만 2배가 됩니다).

> `requests`의 `timeout=40`은 **연결과 응답 각각**에 적용됩니다.
> 우리는 `timeout=(5, 40)` — 연결 5초, 읽기 40초 — 로 나눠 줬습니다.

### 8.4 사용자 안내 문구표 (`js/api.js`의 `MESSAGES`)

**모든 문구는 한 군데에서 관리합니다.** 서버가 준 `message`를 그대로 쓰지 않고
**코드(`code`)로 프론트에서 문구를 고릅니다.** (서버 문구가 영어로 새어 나오는 걸 막습니다.)

| code | 화면 문구 | 버튼 |
|------|-----------|------|
| `EMPTY_INPUT` | 되새길 노트를 먼저 붙여넣어 주세요. | — |
| `TOO_SHORT` | 내용이 너무 짧아요. 100자 이상 붙여넣으면 요약과 퀴즈를 만들 수 있어요. | — |
| `TOO_LONG` | 한 번에 4,000자까지 가능해요. 지금은 {n}자예요 — 나눠서 되새겨 보세요. | — |
| `RATE_LIMITED` | 잠시 쉬어갈까요? 1분 뒤에 다시 시도할 수 있어요. | 다시 시도 |
| `UPSTREAM_RATE_LIMITED` | 지금 이용이 많아요. 30초 뒤에 다시 눌러주세요. | 다시 시도 |
| `UPSTREAM_TIMEOUT` / `CLIENT_TIMEOUT` | 생각이 길어졌어요. 노트를 조금 줄이면 훨씬 빨라져요. | 다시 시도 |
| `UPSTREAM_ERROR` / `INTERNAL` / `NO_API_KEY` / `UPSTREAM_AUTH` | 지금은 되새김이 잠시 쉬고 있어요. 잠시 후 다시 시도해 주세요. | 다시 시도 |
| `INVALID_AI_OUTPUT` | 결과를 다듬는 데 실패했어요. 한 번 더 시도하면 대개 잘 나와요. | 다시 시도 |
| `AI_REFUSED` | 이 내용으로는 퀴즈를 만들기 어려워요. 다른 노트로 시도해 주세요. | — |
| (네트워크 끊김) | 인터넷 연결을 확인해 주세요. | 다시 시도 |

---

## 9. 보안 — API 키 관리

### 9.1 왜 브라우저에서 Claude를 직접 부르면 안 되는가

```js
// ❌ 절대 금지 — 과정이 준 JavaScript 예시를 js/ 에 그대로 옮기면 이렇게 된다
await fetch("https://copa.codyssey.kr/v1/messages", {
  headers: { "x-api-key": "<virtual-key>" }   // 개발자 도구 > 네트워크 탭에 그대로 보인다
});
```

```js
// ✅ 우리 프론트가 부르는 곳은 "우리 서버"다. 키가 등장하지 않는다.
await fetch("/api/summarize", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ note, category, level }),
});
```

**과정이 함께 준 JS 예시는 API의 모양을 설명하기 위한 것입니다.** 그대로 `js/`에 옮기는 순간
virtual key가 공개됩니다. 브라우저로 내려간 것은 **전부 사용자가 볼 수 있습니다.**
JS 파일에 넣든, 난독화하든, 빌드 때 주입하든 결과는 같습니다 — 최종 번들에 문자열로 들어가면 끝입니다.

**그래서 서버(`api/`)가 필요합니다.** 브라우저는 우리 서버에게 "요약해줘"라고만 말하고,
**키를 아는 것은 서버뿐**입니다. 이것이 이 과제에서 Serverless Function을 쓰는 진짜 이유입니다.

> 한 줄 요약: **`api/` 폴더는 "키를 숨길 수 있는 유일한 장소"다.**

### 9.2 환경 변수 목록

| 이름 | 필수 | 기본값 | 용도 |
|------|:----:|--------|------|
| `ANTHROPIC_API_KEY` | ✅ | — | 교육용 **virtual key**. `x-api-key` 헤더로 나간다 |
| `COPA_API_URL` | — | `https://copa.codyssey.kr/v1/messages` | 엔드포인트가 바뀌어도 코드를 안 고치기 위해 뺀다 |
| `CLAUDE_MODEL` | — | `claude-sonnet-4` | 모델 교체 (재배포만 하면 코드 수정 없이 바뀜). 대안: `claude-haiku-4` |
| `RATE_LIMIT_PER_MIN` | — | `5` | 인스턴스당 IP별 분당 허용 횟수 |

### 9.3 설정 방법

**Vercel (배포 환경)**

1. Vercel 대시보드 → 프로젝트 → **Settings → Environment Variables**
2. Key `ANTHROPIC_API_KEY`, Value 붙여넣기, Environment는 **Production + Preview + Development 전부 체크**
3. **저장 후 반드시 재배포** — 환경 변수는 빌드/런타임 시작 시점에 주입되므로 저장만으로는 반영되지 않습니다.
   (Deployments → 최신 배포 → ⋯ → **Redeploy**)

**로컬**

```bash
vercel env pull .env.local     # Vercel에 넣어둔 값을 그대로 내려받는다 (권장)
```

또는 `.env.local`을 직접 만들고 `.env.example`을 참고해 채웁니다.
`vercel dev`가 `.env.local`을 자동으로 읽습니다.

> **`.env.local`은 `.gitignore`에 있어야 합니다.** 이름이 `.env`가 아니라 `.env.local`인 것에 주의 —
> `.gitignore`에 `.env`만 적으면 `.env.local`은 걸러지지 않습니다. **`.env*`로 적으십시오.**

### 9.4 유출 방지 점검 항목 (Day 6과 제출 직전 2회 확인)

- [ ] `js/` 어느 파일에도 키 문자열이 없다 — `grep -rn "x-api-key" js/` 결과가 비어 있다
- [ ] `js/` 어느 파일도 `copa.codyssey.kr`을 직접 호출하지 않는다 (§1.3 🚨)
- [ ] `api/*.py`에 키가 하드코딩돼 있지 않다 (`os.environ`만 사용)
- [ ] `.gitignore`에 `.env*`, `.vercel`, `__pycache__/`가 있다
- [ ] `git log -p | grep -i "x-api-key\|sk-"` 결과에 **실제 값**이 없다 (**이력까지** 확인)
- [ ] README·기획서·PRD에 실제 키 값이 없다
- [ ] **스크린샷의 개발자 도구 네트워크 탭·환경 변수 화면에 키가 찍히지 않았다**
- [ ] 오류 응답 `message`에 예외 원문(`str(e)`)이나 응답 body를 넣지 않았다
- [ ] `/api/health`가 키의 **존재 여부만** 반환한다 (앞자리 일부도 금지)
- [ ] 결과 렌더링에 `innerHTML`을 쓰지 않았다 (XSS)

### 9.5 키가 유출됐을 때 — 순서를 지킬 것

**파일을 고치는 것으로는 해결되지 않습니다.** 이미 push된 순간 이력에 남았고,
공개 저장소라면 봇이 수 분 안에 수집합니다.

| 순서 | 조치 | 왜 이 순서인가 |
|:----:|------|----------------|
| ① | **키 폐기·재발급** — 과정 담당자에게 즉시 알리고 virtual key 교체 | **폐기 없이 이력만 지우는 건 무의미합니다.** 이미 복제된 키는 계속 살아 있습니다 |
| ② | Vercel 환경 변수를 새 키로 교체 → **재배포** | 저장만으로는 반영되지 않습니다 (§9.3) |
| ③ | 서비스가 다시 동작하는지 `/api/health` + T9로 확인 | 여기까지가 **긴급 조치**입니다 |
| ④ | 이력 정리 | 아래 3안 중 택1 |

**④ 이력 정리 3안** — 과제 규모에서는 대개 3안이 가장 안전합니다.

| 안 | 방법 | 주의 |
|----|------|------|
| 1 | 커밋이 **가장 최근 1개**뿐이면 `git reset --soft HEAD~1` → 파일 제외 후 재커밋 → `git push --force` | 이미 남이 clone/fork했다면 소용없음 |
| 2 | `git filter-repo --path .env --invert-paths` 로 파일을 이력에서 제거 → `git push --force` | 별도 설치 필요. **커밋 해시가 전부 바뀜** |
| 3 | **저장소를 새로 만들고** 깨끗한 작업 트리만 첫 커밋으로 올린 뒤 기존 저장소 삭제 | 커밋 이력이 사라짐 → **제출물 2번(커밋 이력)이 약해짐.** 남은 기간에 다시 쌓을 수 있는지 보고 결정 |

> **가장 좋은 대응은 ① `.gitignore`를 코드보다 먼저 커밋하는 것**입니다 (§13 Day 1).
> 그래서 Day 1 체크리스트의 첫 커밋이 `.gitignore`입니다.

### 9.6 쿼터 · 호출 빈도 통제

교육용 virtual key는 **과정에서 정한 한도(쿼터)** 안에서 동작합니다.
정확한 한도·과금 기준은 **과정 안내를 따르고**, 이 문서에는 임의의 금액을 적지 않습니다.
우리가 통제할 수 있는 것은 **호출 1회의 크기**와 **호출 횟수** 둘뿐입니다.

**1회 호출의 크기 (Day 1에 실측해 아래 표를 채운다)**

| 항목 | 예상 | Day 4 실측 |
|------|------|-----------|
| 입력 토큰 (노트 3,950자 + system) | 약 3,000~4,500 | **4,301** ✅ |
| 출력 토큰 (요약+용어+퀴즈 JSON) | 약 1,500 | **1,255~1,669** ✅ |
| 응답 시간 | 약 8~15초 | **haiku 10.5초 / sonnet 19~24초** ⚠️ 예상보다 느렸다 |

> 매 응답의 `usage`를 `meta`에 담아 내려보내고 Vercel 로그에 찍습니다 (§4.4).
> **쿼터가 얼마나 남았는지는 실제 소비량을 봐야 알 수 있습니다.**
> 추정만 하고 넘어가면 마감 전날 한도에 걸립니다.

**쿼터가 부족해지면** — Vercel 환경 변수 `CLAUDE_MODEL`을 **`claude-haiku-4`**로 바꾸고
재배포합니다. 코드는 한 줄도 고치지 않습니다.
(§9.2에서 모델을 상수가 아니라 환경 변수로 뺀 이유가 이것입니다.)

**호출 빈도 통제 3중 장치**

| 층 | 방법 | 한계 |
|----|------|------|
| 입력 | 4,000자 상한 | 1회당 토큰 소비의 상한을 고정 |
| 프론트 | 제출 후 10초 쿨다운 + 버튼 비활성 | 개발자 도구로 우회 가능 |
| 서버 | IP별 분당 `RATE_LIMIT_PER_MIN`회 (`x-forwarded-for` 기준) | **인스턴스 메모리 기반이라 완벽하지 않음** |

> **서버 제한이 완벽하지 않은 이유를 알아야 합니다.** Serverless는 요청마다 다른 인스턴스가
> 뜰 수 있고, 콜드 스타트 때 메모리가 초기화됩니다. 즉 이 방식은 **"실수로 연타하는 사용자"는 막지만
> "작정한 사람"은 못 막습니다.** 제대로 하려면 Vercel KV 같은 외부 저장소가 필요한데, 그건
> 이 과제의 범위 밖이라 **의도적으로 넣지 않았습니다.**
>
> 교육용 키에는 우리가 걸 수 있는 콘솔 한도가 없으므로, **마지막 방어선은 입력 상한과
> 프론트 쿨다운**입니다. 제출 후 배포 URL을 공개적으로 뿌리지 말고 **동료 검증용으로만
> 공유**하는 것도 통제의 일부입니다.

---

## 10. 보너스 A — 다크 모드 & 마이크로 인터랙션

### 10.1 다크 모드

**3단계 우선순위**: 저장된 사용자 선택 > 시스템 설정 > 라이트

```css
:root {                       /* 라이트 = 기본값. 여기에 모든 토큰을 정의한다 */
  --bg: #fbfaf7;  --surface: #ffffff;  --text: #1c1b19;
  --muted: #6b6862; --brand: #2f6f5e; --border: #e5e2da;
}
@media (prefers-color-scheme: dark) {
  :root:not([data-theme="light"]) { --bg: #16181a; --surface: #1f2225; ... }
}
:root[data-theme="dark"] { --bg: #16181a; --surface: #1f2225; ... }
```

> **`:root:not([data-theme="light"])` 가드가 필요한 이유** — 이게 없으면 시스템이 다크인 사용자가
> 수동으로 라이트를 골라도 미디어 쿼리가 덮어써서 **토글이 한쪽 방향으로만 작동**합니다.

- 토글 버튼은 `data-theme` 속성을 `<html>`에 쓰고 `localStorage.theme`에 저장.
- **깜빡임(FOUC) 방지**: `<head>` 안에 인라인 `<script>`로 저장값을 먼저 적용합니다.
  `js/theme.js`를 `<body>` 끝에서 로드하면 흰 화면이 한 번 번쩍입니다.

### 10.2 마이크로 인터랙션 (4개)

| 위치 | 동작 | 구현 |
|------|------|------|
| 로딩 | 결과 자리에 **스켈레톤 카드 3개**가 은은하게 흐름 | CSS `@keyframes shimmer` |
| 버튼 | hover 살짝 올라옴 / active 눌림 / 로딩 중 스피너 + 비활성 | `transform`, `transition 150ms` |
| 결과 | 위에서 아래로 **8px 슬라이드 + 페이드인**, 카드마다 40ms씩 지연 | `animation-delay` |
| 복사·저장 | 화면 하단 **토스트** 2초 후 자동 사라짐 | `role="status"` |

**필수 가드**: `@media (prefers-reduced-motion: reduce)`에서 모든 애니메이션을 끕니다.
전정기관이 예민한 사용자에게 움직임은 불편이 아니라 증상입니다.

### 10.3 "개선 효과를 어떻게 확인할 것인가" (보너스 취지)

측정 도구를 붙이지 않고도 **관측 가능한 형태로** 확인합니다.

| 개선 | 확인 방법 | 기준 |
|------|-----------|------|
| 스켈레톤 로딩 | 동료 3명에게 "언제 멈춘 것 같았나" 질문 (스켈레톤 유/무 각각) | 없을 때 "멈춘 줄 알았다" 응답이 나오는지 |
| 다크 모드 | DevTools 렌더링 탭 → `prefers-color-scheme` 강제 전환 | 두 모드 모두 대비 4.5:1 통과 |
| 글자 카운터 | 4,000자 초과 상태로 제출 시도 | **서버 호출이 0건**인지 네트워크 탭에서 확인 |
| 전체 | Lighthouse (모바일) | Performance·Accessibility 각 90점 이상 목표 |

---

## 11. 보너스 B — 결과 저장 & 알림 자동화

### 11.1 내 기록 (브라우저 저장)

| 항목 | 값 |
|------|-----|
| 저장소 | `localStorage` |
| 키 | `doesaegim.history.v1` (버전을 키에 넣어 스키마 변경 시 충돌 방지) |
| 형태 | `[{ id, created_at, category, level, data }]` — `data`는 §6.3 응답 전체 |
| 상한 | **최근 10건.** 초과 시 오래된 것부터 삭제 |
| 예외 | `QuotaExceededError` → 가장 오래된 것 삭제 후 1회 재시도 → 실패 시 토스트 안내 (E18) |

> 서버 DB를 쓰지 않는 이유: 이 과제에 로그인이 없습니다. 로그인 없이 서버에 저장하면
> **누구의 기록인지 구분할 수 없고**, 남의 노트를 볼 수 있는 구조가 됩니다.
> "저장은 하되 서버에 두지 않는다"가 로그인 없는 서비스의 정직한 선택입니다.

### 11.2 문의 — 폼을 만들었다가 철회한 기록

처음 설계는 **문의 폼 + 외부 웹훅**이었습니다 (`POST /api/feedback` → `WEBHOOK_URL`).
코드는 완성했고 검증도 통과했지만, **보낼 곳을 연결하지 않은 채 배포**됐습니다.
배포 URL에서 실제로 눌러 보니 `503 WEBHOOK_NOT_CONFIGURED`가 나왔습니다.

| 안 | 문제 |
|----|------|
| 웹훅을 연결한다 (Apps Script 등) | 개인 계정에 묶인 URL을 운영해야 하고, **그 URL 자체가 비밀**이라 과제 저장소와 함께 관리하기 부담스럽다 |
| 폼은 두고 "연결되지 않았어요"로 둔다 | **화면에 보이는데 눌러도 안 되는 상태.** 가장 나쁘다 |
| **폼을 없애고 GitHub 이슈로 안내** | ✅ **채택** |

**채택 이유** — 이 서비스는 학습용 과제 프로젝트이고 코드가 공개돼 있습니다.
이슈가 자연스러운 창구이고, **개인정보를 아예 받지 않게 되어** 지킬 것도 줄어듭니다.

폼을 지우면서 `api/feedback.py`·`js/feedback.js`·`WEBHOOK_URL` 환경 변수·
`WEBHOOK_NOT_CONFIGURED` 오류 코드도 함께 지웠습니다.
**죽은 코드를 남겨두는 것보다 없는 편이 정직합니다.** (되살리려면 git 이력에 있습니다.)

> 보너스 1번(운영 자동화 / **데이터 저장 고도화**)은 §11.1의 localStorage 기록으로 충족합니다.

### 11.3 밖으로 나가는 데이터

| 나간다 | 나가지 않는다 |
|--------|---------------|
| AI 처리를 위해 게이트웨이로 보내는 **노트 본문** (저장하지 않음) | 그 밖의 **모든 것** |
| — | 이름·이메일 등 개인정보 (**애초에 받지 않는다**) |
| — | 요약·용어·퀴즈 결과 (브라우저에만) |

**개인정보를 받지 않으면 지킬 것도 없습니다.** 문의 폼을 없애면서 얻은 부수 효과입니다.

---

## 12. 배포 (Vercel)

### 12.1 GitHub 연동

1. GitHub에 `doesaegim` 저장소 생성 (Public — 동료가 접속·리뷰 가능해야 함)
2. Vercel → **Add New → Project → Import Git Repository**
3. **Framework Preset: `Other`** ← 자동 감지에 맡기지 말고 직접 확인
4. Root Directory: `./` , Build Command: 비움, Output Directory: 비움
5. Environment Variables에 `ANTHROPIC_API_KEY` 입력 후 Deploy
6. 이후 `git push` → **자동 재배포**

### 12.2 설정 파일

```json
// vercel.json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "api/*.py": { "maxDuration": 100 }
  }
}
```

```
# .python-version
3.12
```

```
# requirements.txt
requests>=2.31.0,<3.0.0
```

> `requirements.txt`에 **`fastapi`/`flask`/`django`를 넣지 마십시오** (§4.5).
> `python-dotenv`도 불필요합니다 — Vercel이 환경 변수를 직접 주입하고, 로컬은 `vercel dev`가 `.env.local`을 읽습니다.
> `anthropic` SDK도 넣지 않습니다 — 게이트웨이 규격(§1.3)이 `requests` 기준이고,
> 의존성이 적을수록 콜드 스타트가 빠릅니다.

### 12.3 로컬 환경 vs 배포 환경 (과제 목표 대응)

| 항목 | 로컬 | 배포 |
|------|------|------|
| 정적 파일 | 내 PC의 파일 | Vercel CDN (전 세계 캐시) |
| `api/*.py` | `vercel dev`가 흉내 냄 | 실제 서버리스 함수 |
| 환경 변수 | `.env.local` 파일 | Vercel 대시보드 |
| Python | 내 PC에 설치된 버전 | `.python-version`의 3.12 |
| 패키지 | 내 PC의 site-packages | `requirements.txt`로 매 배포 설치 |
| 주소 | `http://localhost:3000` | `https://<project>.vercel.app` |
| 캐시 | 없음 | **있음** — CSS를 고쳤는데 안 바뀌면 강력 새로고침(Ctrl+Shift+R) |

**로컬 실행 3가지 방법**

| 방법 | 명령 | AI 기능 | 용도 |
|------|------|:------:|------|
| ① 전체 | `npm i -g vercel` → `vercel dev` | ✅ | 정식 확인. **배포 전 반드시 1회** |
| ② 화면만 | `python -m http.server 8000` | ❌ | CSS/레이아웃 작업 (`/api` 404) |
| ③ 목업 | `python -m http.server 8000` + URL에 `?mock=1` | 🎭 | **키·네트워크 없이** 결과 화면·스크린샷 작업 |

> **③ 목업 모드**는 A2-1의 `USE_MOCK`을 프론트로 옮긴 것입니다.
> `js/api.js`가 `?mock=1`을 감지하면 `js/mock.js`의 고정 응답을 1.2초 지연 후 반환합니다.
> 로딩 스켈레톤·오류 화면까지 `?mock=timeout`, `?mock=error`로 확인할 수 있어
> **실패 화면 스크린샷을 인위적으로 만들 수 있습니다** (§14.3).

### 12.4 배포 후 문제 → 수정 → 재배포 흐름

```
증상 발견
  │
  ├─ 화면이 아예 안 뜬다 ────────► Vercel Deployments → Build Logs
  │                                (정적 사이트는 빌드가 거의 실패하지 않음 → 경로 오타 의심)
  │
  ├─ /api/... 가 404 ───────────► ① 파일명·경로 확인 (api/summarize.py → /api/summarize)
  │                                ② requirements.txt에 프레임워크가 들어갔는지 (§4.5)
  │                                ③ 클래스 이름이 소문자 handler 인지
  │
  ├─ /api/... 가 500 ───────────► /api/health 호출
  │                                has_api_key: false → 환경 변수 미설정 + 재배포 누락
  │                                true인데도 500 → Runtime Logs에서 예외 확인
  │
  ├─ 로컬은 되는데 배포만 실패 ──► 환경 변수 / 패키지 / 대소문자 파일명
  │                                (Vercel은 Linux — Windows와 달리 대소문자를 구분한다)
  │
  └─ CSS/JS가 옛날 것 ──────────► 강력 새로고침 → 그래도면 파일명에 ?v=2 쿼리 추가

수정 → git commit → git push → 자동 재배포 → 배포 URL에서 재확인
```

> **Vercel은 Linux이고 Windows는 대소문자를 구분하지 않습니다.** `css/Base.css`로 저장해 놓고
> `href="css/base.css"`로 쓰면 **내 PC에선 되고 배포에선 404**입니다. 배포만 깨지는 사고 1위입니다.

---

## 13. 7일 실행 계획

각 Day 끝에 **커밋 + push**하고, Day 2부터는 **매일 배포 URL에서 확인**합니다.
"마지막 날 한 번에 배포"는 이 과제에서 가장 위험한 계획입니다.

### Day 1 — 8/26(수) 기획 확정 & 키 발급

- [ ] 이 PRD 검토, §0 확정 사항 재확인
- [ ] 교육용 **virtual key** 확보, `.env.local`에 넣기 (§9.3)
- [ ] `pip install requests`
- [ ] **소통 확인 1** — §1.3의 `curl`을 그대로 실행해 200과 `content[0].text`를 눈으로 본다
- [ ] **소통 확인 2** — §1.3의 Python(`requests`) 코드를 실행해 같은 결과를 얻는다
- [ ] **모델 2종 확인** — `claude-sonnet-4` / `claude-haiku-4` 둘 다 200이 오는가?
      (하나만 열려 있을 수도 있다. 열린 쪽을 기본값으로 확정한다)
- [ ] **실패 모양 확인** — 키를 한 글자 바꿔 호출해 **어떤 상태 코드와 body가 오는지** 기록한다
      (§7.4 오류 코드 매핑이 이 관찰에서 정확해진다)
- [ ] **응답 길이 확인** — 노트 4,000자를 넣고 `usage`와 응답 시간을 잰다 → §9.6 표를 채운다
- [ ] GitHub 저장소 생성 + `.gitignore` **먼저** 커밋
- [ ] 관찰 결과를 `CLAUDE.md`의 "측정으로 확정된 것"에 기록
- [ ] 📝 **오늘 AI 도구와 나눈 대화를 `docs/ai-tool-log/`에 저장** (아래 상자 참조)
- 산출: 저장소, 동작 확인된 첫 호출, **실패 응답의 실제 모양**

> 📝 **AI 코딩 도구 로그는 매일 저장합니다 — 마지막 날에 만들 수 없습니다.**
> 제출물 5번이 요구하는 것은 "결과물"이 아니라 **"사용 과정"**입니다.
> 특히 `03_오류해결_대화.md`는 **오류가 난 그 순간에만 존재합니다.** 고치고 나면
> 무엇이 왜 안 됐는지 대화가 사라지고, 나중에는 재구성할 수 없습니다.
> Day 2~6의 마지막 체크박스가 전부 이 항목인 이유입니다.

> ⚠️ Day 1에 **`temperature`나 `output_config`를 실험해 볼 필요는 없습니다.**
> 규격(§1.3)에 없는 필드이므로 우리는 보내지 않기로 확정했습니다.
> 대신 **키를 틀렸을 때 무엇이 오는지**를 보는 것이 훨씬 값집니다 —
> §8.2 오류 표가 추측이 아니라 관찰이 됩니다.

### Day 2 — 8/27(목) 뼈대 + **빈 껍데기 배포** ⭐ 가장 중요한 날

- [ ] 폴더 구조 생성 (§4.2), `index.html` 4개 섹션 뼈대 + 네비
- [ ] `api/health.py` 작성 (`_shared.py` import 검증 포함 — §4.2 주의)
- [ ] `vercel.json`, `.python-version`, `requirements.txt`
- [ ] **Vercel 연동 + 배포 → `/api/health`가 브라우저에서 JSON을 반환하는 것 확인**
- [ ] 환경 변수 등록 후 재배포 → `has_api_key: true` 확인
- [ ] 📝 오늘 AI 도구 대화 저장 (배포 오류를 겪었다면 `03_오류해결_대화.md`에)
- 산출: **동작하는 배포 URL** (내용은 비어 있어도 됨)
- 💡 배포는 마지막이 아니라 **두 번째 날**에 끝내는 작업입니다. 배포가 되는 걸 확인한 뒤에
  기능을 얹으면, 이후 실패의 원인은 항상 "방금 내가 쓴 코드"로 좁혀집니다.

### Day 3 — 8/28(금) 프론트 화면 + 라우팅

- [ ] `css/base.css` CSS 변수 · 타이포 · 라이트 팔레트
- [ ] `js/router.js` 해시 라우팅 + 네비 활성화 + 모바일 햄버거
- [ ] 홈 / 내 기록(빈 상태) / 가이드 섹션 마크업
- [ ] `js/mock.js` + `?mock=1` 경로 — 결과 화면을 서버 없이 그린다
- [ ] `images/logo.svg` + `og-cover.png` 제작 → `<head>`의 og 메타 태그 연결
- [ ] 반응형 1차 (375 / 768 / 1280)
- [ ] 📝 오늘 AI 도구 대화 저장
- 산출: 네비게이션이 동작하는 4섹션 화면

### Day 4 — 8/29(토) AI 기능 ⭐ 실패가 가장 많은 날

- [ ] `api/summarize.py` — 입력 검증 → Claude 호출 → 4단계 응답 처리(§6.4) → JSON 응답
- [ ] `js/api.js` `callApi` (타임아웃 · 에러 코드 매핑, throw 하지 않는 계약)
- [ ] `js/study.js` 폼 제출 → 로딩 → 결과 렌더 → 퀴즈 채점
- [ ] `vercel dev`로 로컬 확인 → **배포 후 배포 URL에서 재확인**
- [ ] 노트 10건을 넣어보고 **파싱 실패·검증 실패 빈도**를 세어 `CLAUDE.md`에 기록
- [ ] 📝 오늘 AI 도구 대화 저장 — **오늘이 `03_오류해결_대화.md`의 핵심 재료가 나오는 날**
- 💡 예상 함정: 소문자 `handler` / `rfile.read(length)` /
  `system`을 `messages`에 넣기 / `max_tokens` 누락 / `data["choices"]`로 읽기 /
  코드블록이 붙어 와서 `json.loads` 실패 / 한글 인코딩 / 프리셋 오감지로 인한 `/api` 404

### Day 5 — 8/30(일) 실패 처리 + 반응형 마감

- [ ] E1~E18 전부 구현 (§8.2) — 특히 **요건 5의 3종 전부**
- [ ] `?mock=timeout`, `?mock=error`로 실패 화면 확인
- [ ] IP 기반 호출 제한 + 프론트 쿨다운
- [ ] 반응형 마감 + 접근성 점검 (§5.4)
- [ ] 실제 휴대폰으로 배포 URL 접속 확인
- [ ] 📝 오늘 AI 도구 대화 저장
- 산출: 실패해도 사용자가 무엇을 해야 할지 아는 화면

### Day 6 — 8/31(월) 보너스 + 문서화

- [ ] 다크 모드(FOUC 방지 포함) + 마이크로 인터랙션 4종 (§10)
- [ ] `js/history.js` localStorage 기록 (§11.1)
- [ ] `README.md` 작성 (소개·스택·배포 URL·실행 방법·환경 변수)
- [ ] `docs/서비스기획서.md` 작성 (§3 + §6 + §8 발췌)
- [ ] **§9.4 키 유출 점검 1회차**
- [ ] 📝 오늘 AI 도구 대화 저장
- 산출: 제출 문서 3종 초안

### Day 7 — 9/1(화) 검증 & 제출

- [ ] §14.1 T1~T18 전부 실행
- [ ] 스크린샷 **9장** 촬영 (§14.3) — **키가 화면에 없는지 확인하며 촬영**
- [ ] **README에 스크린샷 삽입** (Day 6에는 아직 사진이 없었다)
- [ ] Day 1~6에 모아둔 AI 도구 대화를 3개 파일로 **정리·편집** → `docs/ai-tool-log/`
      (새로 만드는 게 아니라 **모아둔 것을 다듬는 작업**입니다)
- [ ] §2.5를 근거로 README의 **"설계 결정"** 절 마무리
- [ ] §15 제출물 체크리스트 전부 확인
- [ ] **§9.4 키 유출 점검 2회차** (`git log -p` 포함)
- [ ] 동료 1명에게 배포 URL 전달 → **내 PC가 아닌 곳에서 재현되는지** 확인
- 산출: 제출 패키지 5종

---

## 14. 검증

### 14.1 수동 테스트 시나리오

| ID | 시나리오 | 기대 결과 | 서버 필요 |
|----|----------|-----------|:--------:|
| T1 | 네비 4개를 순서대로 클릭 | 섹션 전환 + 현재 메뉴 강조 + URL 해시 변경 | ❌ |
| T2 | T1 후 **뒤로 가기** 3회 | 이전 섹션으로 되돌아감 | ❌ |
| T3 | `#/asdf`로 직접 접속 | 홈으로 폴백 (빈 화면 아님) | ❌ |
| T4 | 375px에서 햄버거 → 링크 클릭 | 메뉴가 닫히며 이동, 가로 스크롤 없음 | ❌ |
| T5 | 다크 토글 → 새로고침 | 선택이 유지됨, **깜빡임 없음** | ❌ |
| T6 | 빈 상태로 [되새기기] | `EMPTY_INPUT` 안내, **네트워크 요청 0건** | ❌ |
| T7 | 50자 입력 후 제출 | `TOO_SHORT` 안내 | ❌ |
| T8 | 4,500자 붙여넣기 | 카운터 빨강 + `TOO_LONG`, **잘리지 않음** | ❌ |
| T9 | **정상 노트 800자 제출** | 요약 3 · 용어 3~5 · 퀴즈 5 표시 | ✅ |
| T10 | T9 결과에서 [채점하기] | 점수 + 정오 아이콘 + 해설 펼침 | ✅ |
| T11 | T9 직후 [내 기록] | 방금 결과가 목록에 있음 | ✅ |
| T12 | 기록 삭제 → 새로고침 | 삭제 상태 유지 | ❌ |
| T13 | `?mock=timeout` | 타임아웃 안내 + [다시 시도] (**요건 5-③**) | ❌ |
| T14 | `?mock=error` | API 오류 안내 + [다시 시도] (**요건 5-②**) | ❌ |
| T15 | Vercel 환경 변수를 **일부러 틀린 키로** 바꾸고 재배포 후 제출 | 사용자에겐 "일시적 문제", `/api/health`는 `true`, 로그엔 인증 실패 | ✅ |
| T16 | 연속 7회 제출 | 6회째부터 `RATE_LIMITED` | ✅ |
| T17 | `GET /api/summarize` 직접 접속 | 405 + JSON (HTML 오류 페이지 아님) | ✅ |
| T18 | 문의 폼 제출 (웹훅 설정 후) | 시트/알림에 도착, 노트 원문 미포함 | ✅ |

> **T15가 이 과제의 핵심 테스트입니다.** "키가 틀렸을 때 사용자 화면이 어떻게 되는가"를
> 실제로 만들어 확인하지 않으면, 채점 당일 그 상황이 처음 발생합니다.
> 확인 후 **반드시 올바른 키로 되돌리고 재배포**하십시오.

### 14.2 반응형 확인 (요건 4)

| 기기/폭 | 방법 | 확인 |
|---------|------|------|
| 375×812 | DevTools iPhone 15 + **실제 휴대폰** | 1열, 햄버거, 가로 스크롤 없음, textarea 확대 없음 |
| 768×1024 | DevTools iPad | 2열 카드, 가로 메뉴 |
| 1280×800 | 브라우저 실제 창 | 3열, 최대 폭 중앙 정렬 |

### 14.3 스크린샷 계획 (제출물 5번)

`docs/screenshots/NN_이름.png` — 번호 규칙은 A1-2와 동일합니다.

| # | 파일명 | 내용 |
|---|--------|------|
| 01 | `01_home_desktop.png` | 홈 (데스크톱 1280) |
| 02 | `02_study_input_desktop.png` | 노트 입력 상태 |
| 03 | `03_study_loading.png` | **로딩 스켈레톤** (AI 동작 중) |
| 04 | `04_study_result_desktop.png` | **AI 결과** — 요약·용어·퀴즈 |
| 05 | `05_quiz_graded.png` | 채점 결과 + 해설 |
| 06 | `06_home_mobile.png` | 홈 (모바일 375) |
| 07 | `07_study_result_mobile.png` | AI 결과 (모바일 375) |
| 08 | `08_error_timeout.png` | 실패 안내 화면 (`?mock=timeout`) |
| 09 | `09_dark_mode.png` | 다크 모드 (보너스 A) |
| 10 | `10_history.png` | 내 기록 — localStorage 저장 (보너스 B) |

**촬영 완료 (2026-08-31).** 10장 전부 배포 URL에서 촬영했고, 키·개발자도구·환경 변수 화면이 찍히지 않은 것을 확인했습니다.

**촬영 전 필수 확인**: 브라우저 확장 아이콘·다른 탭 제목·Vercel 환경 변수 화면·
개발자 도구 네트워크 탭 헤더에 **키가 찍히지 않았는가.**

**AI 코딩 도구 증빙** — `docs/ai-tool-log/`

| 파일 | 내용 |
|------|------|
| `01_기획_대화.md` | 아이디어·페이지 구성을 정하는 과정 |
| `02_구현_대화.md` | 프론트/백엔드 코드를 생성시킨 프롬프트와 응답 |
| `03_오류해결_대화.md` | **오류 원인을 파악하고 고친 과정** ← 과제 목표 6번의 증빙 |

> 03번이 가장 중요합니다. "AI가 짜준 코드가 왜 안 됐고, 무엇을 보고 원인을 알았고,
> 어떻게 고쳤는지"가 이 과제가 확인하려는 능력입니다. **성공한 대화보다 실패한 대화를 남기십시오.**

---

## 15. 제출물 체크리스트

**1. 배포된 웹 서비스**
- [ ] Vercel URL이 열린다 (내 PC가 아닌 곳에서도)
- [ ] 섹션 4개 + 메뉴 이동
- [ ] 모바일에서 레이아웃이 깨지지 않음
- [ ] AI 기능: 입력 → 결과 출력까지 동작
- [ ] 실패 처리 3종(빈 입력 / API 오류 / 타임아웃) 전부 안내 문구 노출

**2. GitHub 저장소**
- [ ] 코드 전체 업로드, Public
- [ ] 프론트(`index.html`·`css/`·`js/`) ↔ 백엔드(`api/`) 구분
- [ ] 기능 단위 커밋 이력
- [ ] `.gitignore`에 `.env*`·`.vercel`·`__pycache__/`

**3. README.md**
- [ ] 서비스 소개 / 기술 스택 / **배포 URL** / 실행 방법 / 배포 방법
- [ ] 환경 변수 설정 방법 (`.env.example` 안내 포함)
- [x] 스크린샷 삽입
- [ ] 설계 결정 절 ("왜 이렇게 만들었는가") — A1-1부터의 관례

**4. 서비스 기획서** (`docs/서비스기획서.md`)
- [ ] 목적 / 타겟 사용자 / 페이지 구성 / 핵심 기능
- [ ] **AI 기능의 입력 / 출력 / 실패 처리 기준**

> **기획서는 PRD의 요약본이 아니라 독립 문서입니다.** 채점자는 PRD를 읽지 않습니다.
> 아래 목차대로 쓰면 요건 4가지가 빠지지 않습니다 (Day 6, 분량 2~3쪽 권장).
>
> | 절 | 내용 | PRD에서 가져올 곳 |
> |----|------|------------------|
> | 1. 서비스 개요 | 이름·한 줄 정의·해결하려는 문제 | §3.1 |
> | 2. 타겟 사용자 | 1·2순위 사용자, 현재 불편, 대표 사용 시나리오 | §3.2 |
> | 3. 페이지 구성 | 4개 섹션 표 + **메뉴 이동 방식**(해시 라우팅) | §3.3 |
> | 4. 핵심 기능 | 기능 표 + 화면 와이어프레임 | §3.4, §5.2 |
> | 5. AI 기능 명세 | **입력**(필드·검증 규칙) / **출력**(JSON 구조) / **가치** | §6.1, §6.3, §3.5 |
> | 6. 실패 처리 기준 | 빈 입력 · API 오류 · 타임아웃 **3종의 화면 문구** | §8.2, §8.4 |
> | 7. 기술 구성 | 요청 흐름 그림 1장 + 키를 서버에 두는 이유 | §4.1, §9.1 |
>
> 5·6절이 과제가 명시적으로 요구한 항목입니다. **표로 넣으십시오** — 산문으로 풀면
> "입력/출력/실패 기준이 다 있는가"를 채점자가 확인하기 어렵습니다.

**5. 증빙 자료**
- [x] 스크린샷 (데스크톱 + 모바일 + AI 동작 장면) — 10장
- [x] AI 코딩 도구 사용 로그 3종

**보안 (전 항목 공통)**
- [ ] §9.4 점검 항목 전부 통과

---

## 16. 커밋 메시지 컨벤션

접두어 규칙은 A1-1부터 이어지는 관례이고, **커밋 이력을 남기는 것 자체는
이번 과제의 채점 요건입니다** (제출물 2번). 앞선 과제들과 달라지는 지점입니다.

| 접두어 | 용도 |
|--------|------|
| `feat:` | 새 기능 |
| `fix:` | 버그 수정 |
| `style:` | CSS·레이아웃 |
| `docs:` | 문서 |
| `refactor:` | 동작 변화 없는 정리 |
| `chore:` | 설정·의존성 |
| `deploy:` | 배포 설정 (`vercel.json` 등) |

**예시**
```
feat: 노트 요약·퀴즈 생성 API(/api/summarize) 구현
fix: Vercel에서 api 함수가 404 나던 문제 — 클래스명을 소문자 handler로 수정
style: 375px에서 퀴즈 보기가 넘치던 문제 수정
```

> **마지막에 한 번에 올리지 마십시오.** 커밋이 1개뿐인 저장소는 "코드가 있다"는 것만
> 보여줄 뿐 "만들어 온 과정"을 보여주지 못합니다. Day별로 최소 1회 이상 커밋합니다.
> §13의 각 Day 끝에 커밋·push가 들어 있는 이유입니다.

---

## 17. 과제 목표 대응 — "설명할 수 있어야 한다"

| 목표 | 어디서 답이 나오는가 | 한 줄 답 |
|------|---------------------|----------|
| HTML/CSS/JS의 역할 | §4.1, `index.html`/`css/`/`js/` 분리 | 구조 / 표현 / 동작. 셋 다 브라우저가 실행한다 |
| 입력 → fetch → 응답 → 화면 | §4.1 ①~⑩ | submit 이벤트가 값을 읽고 → fetch가 HTTP 요청으로 바꾸고 → 응답 JSON을 DOM으로 만들어 붙인다 |
| Serverless Function이란 | §4.4, §7.5 | 서버를 계속 켜두지 않고, 요청이 올 때만 실행되는 함수. `api/x.py` → `/api/x` |
| 프론트에서 백엔드(Python) 호출 구조 | §4.1, §12.3 | 같은 도메인이므로 `fetch('/api/summarize')`. Vercel이 정적 파일과 함수를 같은 주소에서 서빙한다 |
| 환경 변수로 키를 관리하는 이유 | §9.1 | 브라우저로 내려간 것은 전부 보인다. 키를 아는 곳이 서버뿐이어야 한다 |
| 로컬 vs 배포 차이 / 재배포 흐름 | §12.3, §12.4 | 환경 변수·Python 버전·대소문자·캐시가 다르다. 수정 → push → 자동 재배포 → **배포 URL에서** 재확인 |
| AI 생성 코드의 오류 원인 파악 | §12.4 진단 트리, `docs/ai-tool-log/03` | 증상(404/500/빈 화면)마다 볼 곳이 정해져 있다: Build Log → `/api/health` → Runtime Log |

---

## 18. 리스크 & 대응

| 리스크 | 가능성 | 영향 | 대응 |
|--------|:-----:|:----:|------|
| `/api`가 404 (프리셋 오감지 / 클래스명 대문자) | 높음 | 치명 | **Day 2에 빈 껍데기 배포로 미리 확인** (§13 Day 2) |
| `_shared.py` import 실패 | 중간 | 중간 | Day 2에 검증 → 실패 시 자체 완결로 전환 (§4.2) |
| 응답이 느려 사용자가 이탈 | 높음 | 중간 | 4,000자 상한 + 스켈레톤 로딩 + 예상 시간 안내. 느리면 `claude-haiku-4`로 교체 |
| 교육용 쿼터 소진 | 중간 | **높음** | 입력 4,000자 상한 + 3중 호출 제한 + `claude-haiku-4` 교체. **Day 4에 실제 usage를 재서 남은 여유를 파악** (§9.6) |
| 게이트웨이가 규격 외 필드를 거부 | 중간 | 중간 | 규격에 있는 4개 필드만 보낸다 (§6.3). Day 1에 실호출로 확정 |
| 키 유출 | 낮음 | **치명** | §9.4를 2회 점검. 유출 시 **폐기 우선** |
| Windows/Linux 대소문자 차이로 배포만 깨짐 | 중간 | 높음 | 파일명 전부 소문자·하이픈 규칙 (§12.4) |
| 모바일 레이아웃 붕괴 | 중간 | 높음 | Day 3·Day 5 두 번 확인 + **실제 휴대폰** |
| AI가 노트에 없는 내용을 지어냄 | 중간 | 중간 | 프롬프트 규칙 1 + 100자 하한 + 가이드에 "노트 기반"임을 명시 |
| 마감일에 Vercel·게이트웨이 장애 | 낮음 | 높음 | Day 6까지 기능 완성. 마지막 날은 검증·문서만. `?mock=1`로 화면 증빙은 확보 가능 |

---

## 부록 A. `api/summarize.py` 뼈대 (Day 4 시작점)

> 그대로 붙여넣어 쓰지 말고, 각 블록이 왜 있는지 설명할 수 있을 때 채우십시오.
> 호출 형태는 §1.3의 규격을 그대로 따릅니다.

```python
"""POST /api/summarize — 학습 노트를 요약·용어·퀴즈로 변환한다. (PRD §6, §7.1)

계약:
    성공 → 200 {"ok": true, "data": {...}, "meta": {...}}
    실패 → 4xx/5xx {"ok": false, "code": "...", "message": "..."}
    ★ 어떤 경우에도 HTML 오류 페이지나 예외 원문을 내보내지 않는다 ★
"""
import json
import os
import time
from http.server import BaseHTTPRequestHandler

import requests

API_URL = os.environ.get("COPA_API_URL", "https://copa.codyssey.kr/v1/messages")
MODEL = os.environ.get("CLAUDE_MODEL", "claude-sonnet-4")
MIN_LEN, MAX_LEN = 100, 4000
TIMEOUT = 25                       # 초 (PRD §8.3 타임아웃 사다리)

SYSTEM_PROMPT = """..."""          # PRD §6.2 + §6.3 출력 형식 문단
RESULT_SCHEMA = {...}              # PRD §6.3 — API로 보내지 않는다. 지시문·검증의 기준


# ── Claude 호출 (PRD §1.3 규격) ──────────────────────────────────
def call_claude(system: str, user: str) -> tuple[dict | None, str | None]:
    """성공 → (응답 JSON, None) / 실패 → (None, 오류코드)

    ★ 예외를 호출자에게 던지지 않는다 ★ — 실패 처리를 한 곳에 모으기 위함
    """
    try:
        r = requests.post(
            API_URL,
            headers={
                "x-api-key": os.environ["ANTHROPIC_API_KEY"],   # ★ 코드에 값을 쓰지 않는다 ★
                "anthropic-version": "2023-06-01",
            },
            json={
                "model": MODEL,
                "max_tokens": 4096,          # 규격상 필수 (PRD §1.3)
                "system": system,            # ★ messages가 아니라 최상위 필드 ★
                "messages": [{"role": "user", "content": user}],
                # ❌ temperature / thinking / output_config 는 규격에 없다 (PRD §6.3)
            },
            timeout=TIMEOUT,
        )
    except requests.exceptions.Timeout:
        return None, "UPSTREAM_TIMEOUT"
    except requests.exceptions.RequestException:
        return None, "UPSTREAM_TIMEOUT"      # 연결 실패도 사용자에겐 같은 안내

    # 상태 코드 분기 — 예외 원문이 아니라 코드만 남긴다 (PRD §9.4)
    if r.status_code in (401, 403):
        print("[summarize] 인증 실패 — Vercel 환경 변수의 virtual key를 확인하십시오")
        return None, "UPSTREAM_AUTH"
    if r.status_code == 429:
        return None, "UPSTREAM_RATE_LIMITED"
    if r.status_code >= 400:
        print(f"[summarize] 게이트웨이 오류 status={r.status_code}")
        return None, "UPSTREAM_ERROR"

    try:
        return r.json(), None
    except ValueError:
        return None, "UPSTREAM_ERROR"        # 200인데 JSON이 아님 (프록시 오류 페이지 등)


# ── 응답에서 텍스트 꺼내기 (PRD §6.4 1~2단계) ────────────────────
def extract_text(payload: dict) -> str | None:
    if payload.get("stop_reason") == "max_tokens":
        return None                          # 잘렸으면 JSON이 깨져 있다
    # [0]으로 바로 집지 않는다 — 텍스트가 아닌 블록이 앞에 올 수 있다
    text = next(
        (b.get("text") for b in payload.get("content", []) if b.get("type") == "text"),
        None,
    )
    return strip_fence(text) if text else None


def strip_fence(s: str) -> str:
    """```json ... ``` 껍데기를 벗긴다. 지시는 요청이지 보장이 아니다."""
    s = s.strip()
    if s.startswith("```"):
        s = s.split("\n", 1)[-1]
        s = s.rsplit("```", 1)[0]
    return s.strip()


class handler(BaseHTTPRequestHandler):          # ★ 소문자 handler ★

    # ── 응답 헬퍼 ───────────────────────────────────────────────
    def _json(self, status: int, payload: dict) -> None:
        body = json.dumps(payload, ensure_ascii=False).encode("utf-8")
        self.send_response(status)
        self.send_header("Content-Type", "application/json; charset=utf-8")
        self.send_header("Content-Length", str(len(body)))
        self.end_headers()
        self.wfile.write(body)

    def _fail(self, status: int, code: str) -> None:
        # message는 로그·디버깅용. 화면 문구는 프론트가 code로 고른다 (PRD §8.4)
        self._json(status, {"ok": False, "code": code, "message": code})

    def do_GET(self):
        self._fail(405, "METHOD_NOT_ALLOWED")

    # ── 본체 ────────────────────────────────────────────────────
    def do_POST(self):
        started = time.time()

        # 1) body 읽기 — 반드시 Content-Length 만큼만
        try:
            length = int(self.headers.get("Content-Length") or 0)
            payload = json.loads(self.rfile.read(length) or b"{}")
        except (ValueError, json.JSONDecodeError):
            return self._fail(400, "BAD_JSON_BODY")

        # 2) 입력 검증 (PRD §6.1) — 프론트를 신뢰하지 않는다
        note = (payload.get("note") or "").strip()
        if not note:
            return self._fail(400, "EMPTY_INPUT")
        if len(note) < MIN_LEN:
            return self._fail(400, "TOO_SHORT")
        if len(note) > MAX_LEN:
            return self._fail(400, "TOO_LONG")

        # 3) 호출 제한 (PRD §9.6) — best-effort
        # if rate_limited(self.headers.get("x-forwarded-for")):
        #     return self._fail(429, "RATE_LIMITED")

        # 4) 키 확인 — 없는 걸 먼저 알면 25초 기다릴 필요가 없다
        if not os.environ.get("ANTHROPIC_API_KEY"):
            return self._fail(500, "NO_API_KEY")

        user_prompt = build_user_prompt(payload, note)

        # 5) 호출 → 파싱. 파싱 실패에 한해 정확히 1회 재시도 (PRD §6.4 3단계)
        result = None
        for attempt in range(2):                 # 0: 첫 시도, 1: 재시도 1회
            resp, code = call_claude(SYSTEM_PROMPT, user_prompt)
            if code:
                return self._fail(STATUS[code], code)

            if resp.get("stop_reason") == "refusal":
                return self._fail(422, "AI_REFUSED")

            text = extract_text(resp)
            if text:
                try:
                    result = json.loads(text)
                    break
                except json.JSONDecodeError:
                    pass

            if attempt == 0:
                print("[summarize] JSON 파싱 실패 — 1회 재시도")
                user_prompt += "\n\n※ 반드시 JSON 객체 하나만 출력한다. 코드블록을 붙이지 않는다."
            else:
                return self._fail(502, "INVALID_AI_OUTPUT")

        # 6) 의미 검증 (PRD §6.4 4단계) — 파싱 성공 ≠ 검증 성공
        if (reason := validate(result)) is not None:
            print(f"[summarize] 결과 검증 실패: {reason}")
            return self._fail(502, "INVALID_AI_OUTPUT")   # ★ 여기서는 재시도하지 않는다 ★

        # 7) 성공
        usage = resp.get("usage") or {}          # 게이트웨이가 안 줄 수도 있으니 .get
        self._json(200, {
            "ok": True,
            "data": result,
            "meta": {
                "model": MODEL,
                "elapsed_ms": int((time.time() - started) * 1000),
                "input_tokens": usage.get("input_tokens"),
                "output_tokens": usage.get("output_tokens"),
            },
        })


# 오류 코드 → HTTP 상태 (PRD §7.4)
STATUS = {
    "UPSTREAM_TIMEOUT": 504,
    "UPSTREAM_AUTH": 500,
    "UPSTREAM_RATE_LIMITED": 429,
    "UPSTREAM_ERROR": 502,
}
```

**이 뼈대에서 반드시 설명할 수 있어야 하는 것**

| 지점 | 질문 |
|------|------|
| `call_claude`가 예외를 안 던짐 | 왜 `try`를 호출한 쪽이 아니라 함수 안에 뒀는가? |
| `for attempt in range(2)` | 왜 재시도가 1회뿐인가? 무한 루프가 아닌 이유는? |
| 6)에서 재시도하지 않음 | 파싱 실패와 검증 실패를 왜 다르게 취급하는가? |
| `print`에 `status_code`만 남김 | 왜 응답 body를 통째로 찍으면 안 되는가? (§9.4) |
| `usage.get(...)` | 왜 `usage["input_tokens"]`로 바로 집지 않는가? |

## 부록 B. 설정 파일

```bash
# .env.example — 실제 키를 여기에 쓰지 마십시오.
# 이 파일은 키 "이름"만 알려주는 견본이며 커밋됩니다.
#   1) cp .env.example .env.local
#   2) 값을 채운다
#   3) .env.local은 .gitignore에 있어 커밋되지 않는다
#
# 실수로 키가 커밋되면 파일을 고쳐도 소용없습니다 — git 이력에 남습니다.
# 과정 담당자에게 알리고 virtual key를 재발급받는 것이 유일한 조치입니다.

# --- 필수 ---
# 과정에서 발급받은 교육용 virtual key. x-api-key 헤더로 나갑니다.
ANTHROPIC_API_KEY=

# --- 선택 ---
# 기본: claude-haiku-4 (실측 10.5초)  /  품질을 올리려면: claude-sonnet-4 (19~24초)
CLAUDE_MODEL=

# 엔드포인트가 바뀔 때만. 기본: https://copa.codyssey.kr/v1/messages
COPA_API_URL=


# IP별 분당 허용 호출 수 (기본 5)
RATE_LIMIT_PER_MIN=
```

```gitignore
# .gitignore
.env
.env.*
!.env.example
.vercel
__pycache__/
*.pyc
.DS_Store
node_modules/
```

> `.env.*`로 적어야 `.env.local`이 걸립니다. `.env`만 적는 것이 흔한 사고입니다.
> `!.env.example`은 견본 파일은 커밋되게 하는 예외 규칙입니다.

---

## 부록 C. `index.html` 뼈대 (Day 2 시작점)

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>되새김 — 노트를 요약과 퀴즈로</title>
  <meta name="description" content="학습 노트를 붙여넣으면 3줄 요약·핵심 용어·퀴즈 5문항으로 되돌려줍니다.">
  <link rel="icon" href="images/logo.svg">
  <!-- 카톡·슬랙으로 배포 URL을 공유할 때 미리보기 카드가 뜬다 (images/ 폴더의 용도) -->
  <meta property="og:title" content="되새김 — 노트를 요약과 퀴즈로">
  <meta property="og:description" content="붙여넣으면, 되새길 것만 남습니다.">
  <meta property="og:image" content="images/og-cover.png">
  <!-- ★ 다크 모드 깜빡임(FOUC) 방지: CSS보다 먼저, 인라인으로 (PRD §10.1) -->
  <script>
    (function () {
      var saved = localStorage.getItem("theme");
      if (saved) document.documentElement.dataset.theme = saved;
    })();
  </script>
  <link rel="stylesheet" href="css/base.css">
  <link rel="stylesheet" href="css/layout.css">
  <link rel="stylesheet" href="css/components.css">
</head>
<body>
  <header class="site-header">
    <a class="logo" href="#/">🌀 되새김</a>
    <button class="nav-toggle" aria-label="메뉴 열기" aria-expanded="false">☰</button>
    <nav class="site-nav" id="site-nav">
      <a href="#/">홈</a>
      <a href="#/study">되새김</a>
      <a href="#/history">내 기록</a>
      <a href="#/guide">가이드 · 문의</a>
    </nav>
    <button class="theme-toggle" aria-label="다크 모드 전환">🌙</button>
  </header>

  <main>
    <section id="home"    class="view" data-route="/">        <!-- PRD §5.2 ① --></section>
    <section id="study"   class="view" data-route="/study">   <!-- PRD §5.2 ② --></section>
    <section id="history" class="view" data-route="/history"> <!-- PRD §5.2 ③ --></section>
    <section id="guide"   class="view" data-route="/guide">   <!-- PRD §5.2 ④ --></section>
  </main>

  <footer class="site-footer">
    되새김 · 2026 AI활용학습 A1-3 · 김재민 ·
    <a href="https://github.com/&lt;계정&gt;/doesaegim">GitHub</a>
  </footer>

  <div id="toast" role="status" aria-live="polite"></div>

  <script src="js/mock.js"></script>
  <script src="js/api.js"></script>
  <script src="js/theme.js"></script>
  <script src="js/router.js"></script>
  <script src="js/history.js"></script>
  <script src="js/study.js"></script>
</body>
</html>
```

> `<script>`를 `</body>` 직전에 두는 이유: HTML이 먼저 파싱돼야 JS가 찾을 DOM이 존재합니다.
> 단 **테마 스크립트만은 `<head>`에 인라인으로** 둡니다 — 화면이 그려지기 **전에** 적용돼야 하기 때문입니다.
> 이 두 문장이 "HTML/CSS/JS의 역할"을 설명하는 가장 좋은 예입니다.
